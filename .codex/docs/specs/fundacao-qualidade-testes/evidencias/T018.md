# Evidência T018 — Cobertura de interface de autenticação, cadastro e senha

**Issue:** #227
**Data:** 2026-09-14
**Revisão do checkout:** `a58cc224c2e6a327434674a0de0585adc62219a2`
**Status do Project:** `In Progress`
**Responsável persistido:** `nathannmvr`

## Escopo e sincronização

A issue foi confirmada em `ifpebj-ti/lab-solos`, no Project 41 (`PROJECT_OWNER=ifpebj-ti`, `PROJECT_NUMBER=41`). Antes do RED, o item já estava atribuído a `nathannmvr` e em `In Progress`; os valores foram verificados novamente por `gh issue view` e `gh project item-list --limit 200`. Nenhuma mutação externa adicional foi necessária.

`tasks.md` não foi editado, conforme a posse do orquestrador. Não houve alteração de produção, commit, push, merge, comentário ou fechamento da issue.

## Alterações realizadas

- `frontend/src/test/setup.ts`: restauração de temporizadores reais após cada caso; limpeza protegida de `localStorage`, `sessionStorage` e cookies; reset de handlers MSW preservado.
- `frontend/src/pages/Login.test.tsx`: login válido com payload normalizado e bloqueio de submissão duplicada.
- `frontend/src/pages/CreateAccount.test.tsx`: confirmação de senha divergente sem criação de conta.
- `frontend/src/pages/ForgotPassword.test.tsx`: sessão residual limpa ao entrar no fluxo e bloqueio de solicitações duplicadas.
- `frontend/src/pages/ResetPassword.test.tsx`: bloqueio de redefinições duplicadas durante a validação do token.
- `frontend/src/pages/ChangePassword.test.tsx`: bloqueio de alterações duplicadas e encerramento seguro do fluxo obrigatório sem sessão.
- `frontend/src/auth/sessionConsumers.test.tsx`: inclusão de `ChangePassword` nos consumidores verificados e contrato do isolamento do setup.
- `frontend/src/integration/Auth.integration.test.ts`: login positivo atravessando MSW, persistência de sessão e destino por perfil.

Todos os novos cenários usam interação observável com Testing Library; o caso de integração usa MSW e não faz chamadas reais de rede.

## RED

Linha de base, antes das alterações: o comando direcionado encontrou **7 arquivos e 39 testes aprovados**.

Após escrever os novos cenários, antes de alterar `setup.ts`, o mesmo comando retornou **exit 1**, com **49 testes aprovados e 1 falha**. A falha foi objetiva no contrato de isolamento: `setup.ts` não continha restauração de temporizadores (`vi.useRealTimers()`), limpeza de storage nem remoção de cookies. Não foi uma falha acidental de compilação ou de ambiente.

## GREEN

Foi implementado somente o cleanup de teste em `frontend/src/test/setup.ts`. O comando direcionado passou com **7 arquivos e 50 testes aprovados**, zero falhas e zero ignorados.

## REFACTOR

Após a passagem, as asserções foram reorganizadas para manter interação observável, a busca do campo de confirmação deixou de usar asserção não nula, `ChangePassword` foi incluído na matriz de consumidores e o contrato estático passou a verificar também `server.resetHandlers()`. A suíte direcionada foi repetida e permaneceu verde: **7 arquivos, 50 testes aprovados**.

## Validações

| Comando | Resultado |
|---|---|
| `npm run test -- --run src/pages/Login.test.tsx src/pages/CreateAccount.test.tsx src/pages/ForgotPassword.test.tsx src/pages/ResetPassword.test.tsx src/pages/ChangePassword.test.tsx src/auth/sessionConsumers.test.tsx src/integration/Auth.integration.test.ts` | **0** — 7 arquivos, 50 testes aprovados |
| `npm run test -- --run` | **0** — 121 arquivos, 574 testes aprovados |
| `npm run lint` | **0** — ESLint sem erros ou warnings |
| `npm run build` | **0** — build Vite concluído |
| `git diff --check` | **0** — sem erro de whitespace |

O comando completo do Vitest emite diagnósticos no console de testes negativos de responsividade (`ResponsiveTable`, colunas desconhecidas e valores sem coluna); esses casos já existiam, foram aprovados e não produziram falha da suíte.

## Lacunas e preexistências

- A execução local ocorreu em Node `24.14.0`/npm `11.9.0`, enquanto os pins do frontend são Node 20/npm 10.8.2; os resultados locais não comprovam equivalência de runtime.
- O build manteve avisos preexistentes sobre `/env.js`, `public/images/laboratory.png` e chunk JavaScript acima de 500 kB.
- A validação CI correspondente está declarada em `.github/workflows/container-ci.yml`: o job `Frontend quality` roda em PR para `develop` nos eventos `opened`, `synchronize` e `reopened`, executando teste, lint e build. Não foi executado um run remoto nesta tarefa.
- A tentativa de exercitar divergência de senha nas páginas que usam `zod` v4 revelou incompatibilidade preexistente com a versão instalada de `@hookform/resolvers` (o `ZodError` não é convertido em erro de formulário). A produção está fora da posse T018 e não foi alterada; as validações de API/feedback já existentes permaneceram cobertas. Correção desse problema exigiria tarefa/escopo próprio.
- O checkout já estava sujo antes da T018, incluindo artefatos de build/backend, `frontend/package.json`, `frontend/vite.config.ts`, arquivos de qualidade e `.tmp`; essas alterações foram preservadas.

## Confirmação final

T018 foi executada sem iniciar outra tarefa. Foram alterados somente os oito caminhos de teste/setup sob responsabilidade e este registro de evidência. `tasks.md` permanece sem edição.
