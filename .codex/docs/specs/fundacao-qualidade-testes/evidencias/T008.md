# Evidência T008 — coleta dos produtores reais e inventário legível

- Data: 2026-09-15
- Tarefa: T008
- Resultado: coleta e renderização reais concluídas em toolchain isolada compatível; pronta para o pai atualizar o estado em `tasks.md`
- Issue: #237 — Melhorar indicadores de Maintainability and Reliability na aba Security and quality
- Project 41: `In Progress`
- Assignee: `nathannmvr`
- Revisão observada: `a58cc224c2e6a327434674a0de0585adc62219a2`
- Working tree: alterado por mudanças preexistentes e por arquivos desta iniciativa

## Escopo executado

O coletor em `.github/scripts/quality_baseline.py` executou os produtores reais por listas de argumentos, em diretórios novos por tentativa: ESLint adicional, descoberta dinâmica de todos os projetos `.csproj` da solução, restore NuGet bloqueado, rebuild .NET com SARIF por projeto e incorporação dos achados funcionais.

A `policy.json` recebeu as classificações explícitas observadas na coleta real (`CA1859` e `SYSLIB1045`). `functional-findings.json` foi validado sem alteração: seis candidatos QC-001 a QC-006, todos `aberto`, sem declarar candidato resolvido. Os achados altos continuam fora de qualquer baseline e bloqueantes.

## RED

1. A tentativa no host Windows recusou a coleta antes dos produtores por divergência real de ambiente: Node `24.14.0` em vez de `20.20.2`, npm `11.9.0` em vez de `10.8.2` e Python `3.14.3` em vez de `3.12.14`. .NET `8.0.419`, Docker `29.7.2` e Compose `5.3.1` estavam compatíveis. Nenhum `current.json` foi aceito dessa tentativa.
2. Na primeira tentativa isolada, após ESLint, restore e rebuild reais, a coleta recusou `SYSLIB1045`/`CA1859` por regra não classificada. O erro foi operacional (código 2) e o arquivo de saída não foi escrito; o relatório parcial permaneceu apenas no diretório exclusivo da tentativa.
3. A repetição real mostrou que os índices `run`/`result` do SARIF variam com a ordem paralela do analisador. Com esses campos, o novo teste `test_sarif_occurrence_is_stable_when_result_position_changes` falhou: a mesma ocorrência produzia `result: 0` e `result: 1`.

## GREEN

- `policy.json` passou a classificar `CA1859` e `SYSLIB1045` como `manutenibilidade`/`media`.
- A normalização SARIF passou a preservar fonte e nível, removendo somente índices transitórios de execução; mensagens, caminhos, linhas, severidades originais, estado e fingerprint permanecem preservados.
- Toolchain isolada legítima usada no Windows:
  - Node oficial `20.20.2`, SHA-256 `dc3700fdd57a63eedb8fd7e3c7baaa32e6a740a1b904167ff4204bc68ed8bf77`, conferido contra `SHASUMS256.txt` do release.
  - npm `10.8.2`, distribuído com o Node acima.
  - CPython standalone Windows x64 `3.12.14`, asset `cpython-3.12.14+20260901-x86_64-pc-windows-msvc-install_only.tar.gz`, SHA-256 `e90c1b6419da3bd812dd73bb3de40287a21abf153438147639ec5e20375ea93f`.
  - .NET SDK `8.0.419`, Docker Engine `29.7.2` e Docker Compose `5.3.1` instalados no host.
- `python -B .github/scripts/check_quality_toolchain.py` com o PATH isolado: todos os pins conferidos; `Quality toolchain OK`.
- `python -B -m unittest discover -s .github/scripts/tests -p test_quality_baseline.py -v`: **18 aprovados**.
- Coleta real final: `python -B .github/scripts/quality_baseline.py collect --output .tmp/quality/T008/isolated-20260915-r4/current.json`: código **0**.
- Render real final: `python -B .github/scripts/quality_baseline.py render --current .tmp/quality/T008/isolated-20260915-r4/current.json --output .tmp/quality/T008/isolated-20260915-r4/inventario.md`: código **0**.
- Repetição em diretório novo `isolated-20260915-r5`: collect e render, ambos código **0**.
- Comparação canônica entre as duas coletas finais: fingerprints/achados idênticos (**257**) e resumo de produtores idêntico (**4**); render repetido do mesmo `current.json`: byte-a-byte idêntico.
- Inventário final: 4 produtores com sucesso — ESLint (**11**), `LabSolos-Server-DotNet8` (**138**), `Tests` (**101**) e `E2ESeed` (**1**); 257 achados totais, 6 funcionais e 21 regras técnicas distintas, todas classificadas.
- Três achados funcionais altos permanecem `aberto`: QC-002, QC-003 e QC-005. Os seis candidatos funcionais continuam sem falsa declaração de correção.
- Regressão complementar no host: `python -B -m unittest discover -s .github/scripts/tests -p 'test_*.py' -q`: **302 aprovados**, 2 skips preexistentes, zero falhas.
- `git diff --check` nos caminhos sob responsabilidade: código **0**.

## REFACTOR

O refactor ficou limitado ao contrato de estabilidade da normalização: a identidade continua baseada em regra, caminho e contexto, enquanto a evidência padrão não incorpora posições transitórias do SARIF. A coleta mantém restore bloqueado antes do rebuild, `--artifacts-path` novo, descoberta dinâmica de projetos, rejeição de relatório ausente/ambíguo, rejeição de JSON inválido e rejeição de regra desconhecida. Nenhum produtor foi ignorado e nenhuma regra foi suprimida.

## Limitações e bloqueios remanescentes

- O host Windows continua incompatível com Node/npm/Python pinados; a coleta concluída usa a variante isolada documentada acima e não falsifica as versões do host.
- A suíte Python completa na mesma distribuição standalone isolada não foi usada como aceite: um teste preexistente externo a T008 falhou ao importar `yaml`, ausente nessa distribuição. A suíte completa passou no host com PyYAML disponível; a suíte direcionada T008 passou no runtime exato.
- Os 3 achados funcionais altos abertos bloqueiam o gate de qualidade e a conclusão global de CA-002; T008 apenas os coleta e preserva para triagem posterior.
- As saídas estão em `.tmp/quality/T008/isolated-20260915-r4/` e `isolated-20260915-r5/`, sem versionamento. Não houve edição de `tasks.md`, frontend, backend, Dockerfiles ou workflows, nem commit, push, merge, PR ou fechamento da issue.
