# Evidencias T016 — Caracterizacao de devolucao

- Tarefa: T016
- Issue: #237
- SHA inspecionado: `a58cc224c2e6a327434674a0de0585adc62219a2`
- Status da evidencia: concluida como diagnostico; QC-001 confirmado alto e aberto
- Escopo: somente `LoanReturnCharacterizationTests.cs`, `QC-001.md` e este arquivo

## Preparacao e sincronizacao

- Dependencia T015 confirmada como `concluida` em `tasks.md`; o arquivo nao foi editado nesta tarefa.
- Issue #237 confirmada aberta, atribuida a `nathannmvr` e vinculada ao Project 41.
- Item do Project 41 confirmado em `In Progress`.
- Nenhum comentario, fechamento da issue, commit, push, merge ou PR foi realizado.

## Caracterizacao planejada

`LoanReturnCharacterizationTests` usa `PostgreSqlIntegrationCollection`, `IntegrationWebApplicationFactory` e `CriticalScenarioData` para:

1. criar dados sinteticos em PostgreSQL com a data prevista da fixture preservada;
2. aprovar o emprestimo por HTTP autenticado como administrador;
3. tentar a devolucao pelo endpoint HTTP real;
4. consultar o estado em novo contexto para observar data e estoque.

O teste primeiro esperou `204 NoContent` na devolucao. A execucao real retornou `400 BadRequest`; depois o teste foi convertido em caracterizacao do comportamento observado e passou, mantendo explicitas as consultas em novo contexto.

## RED/GREEN/REFACTOR

- RED: o filtro executado com a expectativa funcional falhou por comportamento, nao por ambiente: `Expected: NoContent`, `Actual: BadRequest`, 1 falha em `LoanReturnCharacterizationTests.cs:43`.
- GREEN/caracterizacao: apos afirmar o resultado real, o filtro passou com `Aprovado: 1`, `Com falha: 0`, `Ignorado: 0`. A consulta pos-aprovacao e a consulta pos-tentativa confirmaram data prevista preservada e quantidade `8`.
- REFACTOR: a preparacao foi isolada nos helpers `SeedScenarioAsync`, `CreateAuthenticatedClientAsync`, `SendPatchAsync` e `ReadLoanStateAsync`, reutilizando a infraestrutura de T010/T015; a mesma caracterizacao permaneceu verde. Nao houve mudanca de producao.

## Comandos e resultados reais

### Linha de base executada

```text
dotnet test backend/Tests/Tests.csproj --no-restore --no-build -c Release --nologo --disable-build-servers --filter "FullyQualifiedName~LoanReturnCharacterizationTests"
```

Resultado: codigo de saida `0`; nenhum teste correspondeu ao filtro, pois o arquivo T016 ainda nao existia no assembly.

### Validacoes T016 executadas

```text
dotnet restore backend/backend.sln --locked-mode --artifacts-path .tmp/quality/T016/backend
```

Resultado: codigo de saida `0`; `LabSolos-Server-DotNet8.csproj` e `Tests.csproj` restaurados.

```text
dotnet test backend/Tests/Tests.csproj --no-restore --artifacts-path .tmp/quality/T016/backend -c Release --nologo --disable-build-servers --filter "FullyQualifiedName~LoanReturnCharacterizationTests"
```

Resultado RED: codigo de saida `1`; 1 teste falhou por comportamento, com `Expected: NoContent` e `Actual: BadRequest`.

Resultado GREEN/caracterizacao com o mesmo comando: codigo de saida `0`; `Aprovado: 1`, `Com falha: 0`, `Ignorado: 0`.

```text
dotnet test backend/backend.sln --no-build --no-restore --artifacts-path .tmp/quality/T016/backend -c Release --nologo --disable-build-servers
```

Resultado: codigo de saida `0`; `Aprovado: 173`, `Com falha: 0`, `Ignorado: 0`, duracao de 31 s.

```text
git diff --check
```

Resultado: nenhum diagnostico de whitespace; apenas avisos preexistentes de conversao LF/CRLF em arquivos ja modificados fora do escopo T016.

## Bloqueio e proximo passo

Bloqueio funcional confirmado: o fluxo de devolucao e o estoque permanecem impedidos pela colisao entre data prevista e guarda de devolucao. O achado requer contrato complementar para separar data prevista e data efetiva, alem de decisao de migracao e reversao. T016 nao corrige esse comportamento e nenhum codigo de producao deve ser alterado nesta tarefa.
