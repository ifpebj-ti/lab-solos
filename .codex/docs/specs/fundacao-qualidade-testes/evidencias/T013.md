# Evidência T013 — Aprovação e rejeição vinculadas à identidade autenticada

- Data: 2026-09-14
- Issue: #227
- Project: 41
- Status final: `In Progress`
- Assignee validado: `nathannmvr`
- Ambiente: Windows, .NET SDK `8.0.419`, Docker Engine disponível para Testcontainers PostgreSQL

## Escopo executado

As operações HTTP de aprovação e rejeição de dependentes foram cobertas com PostgreSQL real, autenticação JWT e consulta posterior em novo contexto de banco. O controlador passou a derivar o sujeito das claims, exigir consistência do `AprovadorId` compatível e validar o vínculo persistido do responsável. Administradores continuam autorizados pela política existente. A rejeição administrativa passou a retornar `DependenteDTO`, preservando a resposta pública sem credenciais ou campos internos.

## Sincronização da issue e do Project

Antes do RED, foram validados:

- repositório atual: `ifpebj-ti/lab-solos`;
- usuário autenticado: `nathannmvr`;
- issue #227 aberta e pertencente ao repositório atual;
- item da issue no Project 41 com assignee `nathannmvr` e status `In Progress`.

Não foi necessária mutação externa adicional, pois assignee e status já estavam persistidos. A validação final confirmou o mesmo estado.

## RED

Linha de base, após restauração isolada: `UsuarioDataContractTests` encontrou 18 testes aprovados; não existia `CriticalUserApprovalTests`.

Após a criação dos testes de T013, a execução direcionada encontrou 33 casos, com 29 aprovados e 4 falhas comportamentais esperadas:

- responsável A com `AprovadorId` de B no corpo foi aceito com `200` em aprovação e rejeição, expondo QC-002;
- administrador foi recusado com `401` em aprovação e rejeição, embora a política permita o perfil.

Nenhuma falha de compilação ou de ambiente foi usada como RED. Uma tentativa inicial sem restore no diretório de artefatos falhou operacionalmente com `NETSDK1004` por ausência de `project.assets.json`; o restore bloqueado subsequente passou antes da linha de base.

## GREEN

Implementação mínima no `UsuariosController`:

- leitura do ID autenticado por `ClaimTypes.NameIdentifier` ou `sub`;
- recusa quando o ID do corpo diverge da identidade autenticada;
- conferência de `ResponsavelId` contra o sujeito autenticado para mentores;
- preservação da autorização administrativa;
- retorno de `DependenteDTO` na rejeição administrativa.

Resultado: 33/33 testes direcionados aprovados, incluindo aprovação/rejeição correta, administrador, anônimo, perfil proibido, identidade adulterada, alvo inexistente, solicitação processada e contrato público.

## REFACTOR

As duas operações passaram a compartilhar `ValidateDependentDecision`, a mensagem de autorização foi centralizada e os testes passaram a compartilhar a asserção da resposta pública. O mesmo recorte foi executado novamente com 33/33 aprovados.

## Validações da tarefa

| Comando | Resultado |
|---|---|
| `dotnet restore backend/backend.sln --locked-mode --artifacts-path .tmp/quality/T013/backend` | passou |
| `dotnet test backend/Tests/Tests.csproj --no-restore --artifacts-path .tmp/quality/T013/backend -c Release --nologo --disable-build-servers --filter "FullyQualifiedName~CriticalUserApprovalTests\|FullyQualifiedName~UsuarioDataContractTests"` | 33 aprovados, 0 falhas, 0 ignorados |
| `dotnet test backend/backend.sln --no-build --no-restore --artifacts-path .tmp/quality/T013/backend -c Release --nologo --disable-build-servers` | 154 aprovados, 0 falhas, 0 ignorados |
| `git diff --check` | código 0 |

## Falhas preexistentes e limitações

- Avisos históricos de análise .NET permanecem, incluindo `CS8604` em `ProdutoMappingProfile.cs` nas linhas 48 e 50 e regras `CA*`/`CS*` já existentes; não foram alterados nesta tarefa.
- `git diff --check` emite avisos informativos de conversão LF/CRLF em arquivos preexistentes do worktree; não houve erro de whitespace.
- Não foram executados commit, push, merge, fechamento da issue ou outra tarefa.
- `tasks.md` não foi editado.
