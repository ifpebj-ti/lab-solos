# Evidência T020 — Cobrir solicitação de empréstimo pela interface

- Data: 2026-09-14
- Issue: #227
- Project: 41 (`ifpebj-ti`)
- Status final no Project: `In Progress`
- Assignee: `nathannmvr`
- Ambiente: Windows; Node `24.14.0`, npm `11.9.0`, Vitest `4.1.11`
- Pins do repositório: Node `20.20.2` em `frontend/.node-version` e npm `10.8.2` em `frontend/package.json`

## Escopo e contrato

A tela continua exigindo a seleção do usuário e dos produtos, mas o POST envia somente o contrato aceito pela API: `diasParaDevolucao` e `produtos`, com `produtoId` e `quantidade`. O solicitante não é confiado ao corpo do cliente; a integração usa o `doorKey` existente no cookie para o header `Authorization`.

Foram cobertos sucesso, envio pendente sem duplicação, recusas HTTP 400/403/500, indisponibilidade de rede, ausência de sessão e retentativa após falha. A seleção só é limpa após sucesso; erro mantém os dados para correção ou nova tentativa.

## RED

Comando:

```text
npm run test -- --run src/pages/mentor/LoanCreation.critical.test.tsx src/integration/Loans.creation.test.ts --reporter=verbose
```

Resultado: falha intencional em 1 teste. A tela enviava `solicitanteId: 501` além do contrato previsto; os outros 7 testes passaram. A falha foi reproduzida após corrigir uma asserção ambígua do próprio teste.

## GREEN

Alteração mínima em `LoanCreation.tsx` e `Loans.ts`: remoção de `solicitanteId` do payload e do tipo local de criação. O comando direcionado passou com `8 testes aprovados`.

## REFACTOR

Os testes consolidaram o disparo da solicitação e a consulta da lista de produtos selecionados. Foi acrescentado o caso de indisponibilidade de rede, que deve resultar em erro `network` retentável. Reexecução:

```text
npm run test -- --run src/pages/mentor/LoanCreation.critical.test.tsx src/integration/Loans.creation.test.ts --reporter=verbose
```

Resultado: `2 arquivos aprovados, 9 testes aprovados`.

## Validação da tarefa

```text
npm run test -- --run src/pages/mentor/LoanCreation.critical.test.tsx src/pages/mentor/LoanCreation.responsive.test.tsx src/integration/Loans.creation.test.ts src/integration/Loans.contract.test.ts
```

Resultado: `4 arquivos aprovados, 19 testes aprovados`.

Regressão frontend adicional:

```text
npm run test -- --run
```

Resultado: `125 arquivos aprovados, 592 testes aprovados`.

```text
npm run lint
```

Resultado: código `0`, sem erros ou warnings.

```text
npm run build
```

Resultado: código `0`. Permanecem avisos preexistentes sobre `/env.js` sem `type="module"`, `laboratory.png` não resolvida em tempo de build e chunk JavaScript acima de 500 kB.

```text
git diff --check
```

Resultado: código `0`. O Git emitiu somente avisos informativos preexistentes de conversão LF/CRLF.

## Limitações e escopo

O host local não possui os pins Node/npm exigidos, então a validação frontend foi executada com Node 24/npm 11; a divergência já está registrada nas evidências de T004. Não foram alteradas regras de negócio, endpoints backend, cobertura responsiva existente ou `tasks.md`.
