# Evidência T006 — Produzir diagnósticos frontend sem enfraquecer o lint

- **Issue:** #237
- **Data:** 2026-09-14
- **Project:** `LabOn — Desenvolvimento e Qualidade` (`ifpebj-ti/41`)
- **Status inicial/final no Project:** `In Progress`
- **Responsável persistido:** `nathannmvr`
- **Repositório:** `ifpebj-ti/lab-solos`
- **SHA observado:** `a58cc224c2e6a327434674a0de0585adc62219a2`

## Escopo entregue

- `frontend/eslint.quality.config.js` herda integralmente `frontend/eslint.config.js`.
- A configuração adicional acrescenta somente `complexity` com limite `20` e `max-depth` com limite `4`, ambas como warnings (`severity: 1`).
- Código em `src` recebe globals de browser; E2E e arquivos `vite.config.*`/`playwright.config.*` recebem globals de Node.
- A configuração conserva as exclusões da base (`dist` e `node_modules`) e acrescenta `coverage`, `reports`, `test-results`, `playwright-report`, artefatos E2E e `.tmp` como saídas geradas.
- Os testes exercitam ESLint real com o lockfile instalado, relatório JSON, fixtures acima/abaixo dos limites, escopos browser/Node e exclusão de dependências/relatórios.

## Sincronização da issue/Project

Antes do RED foram executados:

```text
gh api user --jq .login
gh variable get PROJECT_OWNER --repo ifpebj-ti/lab-solos
gh variable get PROJECT_NUMBER --repo ifpebj-ti/lab-solos
gh issue view 237 --repo ifpebj-ti/lab-solos --json number,title,state,assignees,projectItems,url
gh project item-list 41 --owner ifpebj-ti --format json --limit 1000
```

Resultado persistido antes da implementação:

- usuário autenticado: `nathannmvr`;
- owner/Project resolvidos: `ifpebj-ti` / `41`;
- Issue #237 pertence ao repositório atual e está aberta;
- assignee: `nathannmvr`;
- item do Project: `In Progress`.

Como os valores já estavam corretos, não foi necessária mutação externa. Não foram publicados comentários, fechada a issue ou alterados outros campos.

## RED

Foram criados primeiro o teste contratual e as fixtures, sem criar a configuração adicional. O comando:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_eslint_analysis.py -v
```

falhou como esperado: 3 testes falharam e 1 apresentou erro de carregamento porque `frontend/eslint.quality.config.js` não existia. O ESLint reportou `ENOENT` para esse arquivo. Isso demonstrou a lacuna da configuração, sem fabricar falha de produção.

## GREEN

Foi adicionada a configuração mínima e executado novamente o teste direcionado. Após ajustar o harness para materializar as fixtures em uma área temporária dentro da raiz do ESLint — preservando as fixtures versionadas fora do frontend — o resultado foi:

```text
Ran 4 tests in 9.978s
OK
```

Os quatro contratos aprovados confirmam:

- fixture acima dos limites produz `complexity` e `max-depth` com severidade warning;
- fixture abaixo dos limites não produz esses IDs;
- `src`, `e2e`, `vite.config.ts` e `playwright.config.ts` são analisados;
- `node_modules` e `reports` são excluídos;
- globals `window` e `process` estão disponíveis nos escopos correspondentes.

## REFACTOR

Depois do GREEN, os padrões de arquivos analisáveis e saídas geradas foram extraídos para constantes nomeadas, mantendo a herança da configuração base e sem duplicar ou desativar suas regras. O teste passou a usar `parse_eslint_json` e formatação consistente das asserções.

A repetição da suíte direcionada após a refatoração permaneceu verde:

```text
Ran 4 tests in 9.978s
OK
```

## Validações finais

| Comando | Resultado |
|---|---|
| `python -B -m unittest discover -s .github/scripts/tests -p test_quality_eslint_analysis.py -v` | **0**, 4 testes aprovados após REFACTOR |
| Em `frontend`: `npm ci` | **0**, 855 pacotes adicionados, 856 auditados, 0 vulnerabilidades |
| Em `frontend`: `npx --no-install eslint src e2e vite.config.ts playwright.config.ts --config eslint.quality.config.js --format json --output-file ../.tmp/quality/T006/eslint.json` | **0**, relatório JSON com 335 arquivos, 0 erros e 11 warnings adicionais; todos os 11 são `complexity` |
| Em `frontend`: `npm run lint` | **0**, zero erros e zero warnings com a configuração obrigatória atual |
| `git diff --check` | **0** |

O relatório JSON foi escrito somente em `.tmp/quality/T006/eslint.json`, artefato não versionado. A análise real encontrou complexidade acima do limite em código existente; esses warnings são o diagnóstico adicional esperado e não foram ocultados nem convertidos em erro do lint obrigatório.

## Preexistências, limitações e riscos

- O checkout já continha alterações de outras tarefas e artefatos de build, incluindo `frontend/package.json` e `frontend/vite.config.ts` da T004; não foram alterados pela T006.
- `git diff --check` exibiu apenas avisos de conversão LF/CRLF para esses arquivos preexistentes; o código T006 não apresentou erro de whitespace.
- O host executa Node `24.14.0`/npm `11.9.0`, enquanto o pin de Node é `20.20.2` e o `packageManager` é npm `10.8.2`; `npm ci` passou no checkout local, mas a equivalência do runtime Node 20 não foi comprovada nesta máquina, conforme limitação já registrada pela T004.
- Os 11 warnings adicionais permanecem como dívida para a coleta/classificação posterior; a configuração adicional os expõe e não enfraquece `npm run lint`.
- Não foram executados build, Vitest ou E2E, pois não fazem parte da validação especificada da T006 e não houve alteração nesses caminhos.

## Confirmação de escopo

Arquivos alterados pela T006:

- `frontend/eslint.quality.config.js`;
- `.github/scripts/tests/test_quality_eslint_analysis.py`;
- `.github/scripts/tests/fixtures/quality/eslint/**`;
- `.codex/docs/specs/fundacao-qualidade-testes/evidencias/T006.md`.

`tasks.md` **não foi editado**. Não houve commit, push, merge, fechamento da issue, comentário externo ou início de outra tarefa.
