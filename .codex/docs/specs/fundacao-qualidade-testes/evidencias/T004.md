# Evidência T004 — Fixar Node/npm e tornar metadados frontend determinísticos

- **Issue:** #227
- **Data:** 2026-09-14
- **Project:** `LabOn — Desenvolvimento e Qualidade` (`ifpebj-ti/41`)
- **Status inicial/final no Project:** `In Progress`
- **Responsável persistido:** `nathannmvr`
- **Issue:** aberta no repositório `ifpebj-ti/lab-solos`

## Escopo entregue

- `frontend/.node-version` fixa Node `20.20.2`, conforme `.github/quality/toolchain.json`.
- `frontend/package.json` declara `packageManager: npm@10.8.2`.
- `frontend/package-lock.json` foi validado por `npm ci` e permaneceu sem diff.
- `frontend/src/test/buildMetadata.ts` separa defaults determinísticos de `test` da identificação de produção; aceita `VITE_APP_VERSION`, `VITE_APP_GIT_HASH` e `VITE_APP_BUILD_DATE`.
- `frontend/vite.config.ts` usa o modo recebido pelo Vite e não consulta release nem relógio em modo `test`; produção mantém fallback de release/hash/data quando não recebe metadados explícitos.
- `frontend/src/test/buildMetadata.test.ts` testa o helper e a configuração Vite efetiva em `test` e `production`.
- `.github/scripts/tests/test_quality_frontend_toolchain.py` verifica os pins e o formato lockfile.

## Sincronização da issue/Project

Antes do RED foram executados `gh api user --jq .login`, `gh variable get PROJECT_OWNER --repo ifpebj-ti/lab-solos`, `gh variable get PROJECT_NUMBER --repo ifpebj-ti/lab-solos` e consultas da issue/Project.

- Usuário autenticado: `nathannmvr`.
- Configuração resolvida: owner `ifpebj-ti`, Project `41`.
- A issue já estava atribuída a `nathannmvr` e o item já estava em `In Progress`; ambos foram confirmados persistidos antes da implementação.
- Ao finalizar, a issue continua aberta e o item continua `In Progress`, pois há tarefas posteriores no plano.
- Não foram adicionados comentários, fechada a issue ou alterados outros campos externos.

## RED

Antes de escrever a implementação, as linhas de base direcionadas demonstraram as lacunas esperadas:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_frontend_toolchain.py -v
Ran 0 tests in 0.000s
NO TESTS RAN
exit 1

npm run test -- --run src/test/buildMetadata.test.ts
No test files found, exiting with code 1
```

Depois da criação dos testes, o Vitest falhou por `buildMetadata.ts` ausente. O contrato Python falhou porque `frontend/.node-version` e `packageManager` ainda não existiam. Esses resultados demonstraram as lacunas de T004; não foram fabricadas falhas de produção.

## GREEN

Foi implementado o mínimo necessário para satisfazer os contratos:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_frontend_toolchain.py -v
Ran 3 tests in 0.004s
OK

npm run test -- --run src/test/buildMetadata.test.ts
Test Files  1 passed (1)
Tests  4 passed (4)
```

O teste de configuração efetiva confirmou que o modo `test` não chama `fetch` e usa os três valores explícitos; o helper confirmou defaults idênticos para entradas idênticas. O modo `production` preservou a identificação explícita sem substituir os valores por constantes de teste.

## REFACTOR

Após GREEN, a resolução foi reorganizada em funções nomeadas para defaults de teste, versão de produção e metadados de produção. O teste passou a reutilizar um carregador da configuração Vite e ganhou a verificação efetiva de produção. A suíte direcionada repetida passou:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_frontend_toolchain.py -v
Ran 3 tests in 0.004s
OK

npm run test -- --run src/test/buildMetadata.test.ts
Test Files  1 passed (1)
Tests  4 passed (4)

npm run lint
exit 0
```

## Validações finais

| Comando | Resultado |
|---|---|
| `python -B -m unittest discover -s .github/scripts/tests -p test_quality_frontend_toolchain.py -v` | **0**, 3 testes aprovados |
| `npm ci` em `frontend` | **0**, 855 pacotes adicionados, 856 auditados, 0 vulnerabilidades; avisos de depreciação preexistentes |
| `npm run test -- --run src/test/buildMetadata.test.ts` | **0**, 4 testes aprovados |
| `npm run test -- --run` | **0**, 121 arquivos e 563 testes aprovados |
| `npm run lint` | **0**, sem erros/avisos ESLint |
| `npm run build` | **0**, bundle produzido |
| Build com `VITE_APP_VERSION=v9.8.7`, `VITE_APP_GIT_HASH=abc1234`, `VITE_APP_BUILD_DATE=2026-09-14T12:34:56.000Z` | **0**, os três valores encontrados em `dist/assets/index-CdxwV-ke.js` |
| `git diff --check` | **0** |

O build continuou emitindo os avisos já existentes sobre `/env.js`, referência não resolvida a `public/images/laboratory.png` e chunk JavaScript acima de 500 kB. Não foram corrigidos por não pertencerem à T004.

## Ambiente, preexistências e limites

- Host observado: Node `v24.14.0` e npm `11.9.0`; não há gerenciador local de versões disponível para executar Node `20.20.2` nesta sessão. Portanto, `npm ci` passou no checkout, mas a equivalência do runtime fixado fica limitada ao contrato dos manifestos; a execução não deve ser apresentada como prova do binário Node 20.
- O `package-lock.json` está em lockfile v3 e não foi alterado.
- Os avisos/artefatos preexistentes do checkout fora da posse da T004 foram preservados.
- A falha inicial ao carregar a API Vite no ambiente padrão `jsdom` foi uma incompatibilidade de `TextEncoder`/`Uint8Array` do ambiente de teste; o teste foi delimitado a `node`, ambiente apropriado para carregar a configuração Vite, e passou.

## Confirmação de escopo

Arquivos alterados pela T004:

- `frontend/.node-version`
- `frontend/package.json`
- `frontend/vite.config.ts`
- `frontend/src/test/buildMetadata.test.ts`
- `frontend/src/test/buildMetadata.ts`
- `.github/scripts/tests/test_quality_frontend_toolchain.py`
- `.codex/docs/specs/fundacao-qualidade-testes/evidencias/T004.md`

`frontend/package-lock.json` permaneceu sem alterações. `tasks.md` **não foi editado**. Não houve commit, push, merge, fechamento de issue, início de outra tarefa ou alteração intencional fora dos caminhos sob responsabilidade da T004.
