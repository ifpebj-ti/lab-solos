# Evidência T015 — decisão de empréstimos e estoque

- Data: 2026-09-14
- Issue: #227
- Project 41: `In Progress`
- Assignee: `nathannmvr`
- SHA sob teste: `a58cc224c2e6a327434674a0de0585adc62219a2`
- Ambiente: .NET SDK `8.0.419`, Docker Engine `29.7.2`, PostgreSQL Testcontainers `postgres:16-alpine`

## Escopo executado

Foi adicionada cobertura HTTP de decisão de empréstimos usando PostgreSQL, `IntegrationWebApplicationFactory`, dados sintéticos determinísticos e novo contexto de leitura após cada operação. Os cenários cobrem:

- aprovação administrativa, persistência do aprovador e baixa única de estoque;
- rejeição administrativa, persistência do aprovador e preservação do estoque;
- perfis não administrativos proibidos;
- ID de empréstimo ausente;
- estoque insuficiente no segundo de dois produtos, sem redução parcial;
- reprocessamento de aprovação e rejeição sem efeito duplicado.

## RED

Linha de base direcionada:

```text
dotnet test backend/Tests/Tests.csproj --artifacts-path .tmp/quality/T015/baseline -c Release --nologo --disable-build-servers --filter "FullyQualifiedName~CriticalLoanDecisionTests|FullyQualifiedName~CriticalLoanCreationTests"
```

Resultado: 3 testes aprovados dos cenários de criação; nenhum teste `CriticalLoanDecisionTests` era descoberto.

Após a criação do teste T015, a execução direcionada encontrou 7 casos, com 6 aprovados e 1 falha esperada:

```text
Expected: 699691
Actual:   null
AdministratorRejectionKeepsStockAndPersistsDecision
```

A falha demonstrou que a rejeição persistia o status, mas não registrava o administrador autenticado em `AprovadorId`. Uma tentativa anterior com `--no-restore` em um diretório de artefatos novo retornou `NETSDK1004` por ausência de `project.assets.json`; após restore no mesmo diretório, o RED comportamental foi reproduzido corretamente.

## GREEN

Foi adicionada somente a atribuição do administrador autenticado a `emprestimo.AprovadorId` no fluxo de rejeição. O teste direcionado passou:

```text
7 aprovados, 0 falhas, 0 ignorados
```

## REFACTOR

As asserções repetidas de decisão persistida e de empréstimo pendente sem mutação foram consolidadas em helpers locais; o comentário do ramo de rejeição foi corrigido para refletir o estado real. A mesma suíte permaneceu verde:

```text
7 aprovados, 0 falhas, 0 ignorados
```

## Validação da tarefa

| Comando | Resultado |
|---|---|
| `dotnet restore backend/backend.sln --locked-mode --artifacts-path .tmp/quality/T015/backend` | aprovado |
| `dotnet test backend/Tests/Tests.csproj --no-restore --artifacts-path .tmp/quality/T015/backend -c Release --nologo --disable-build-servers --filter "FullyQualifiedName~CriticalLoanDecisionTests|FullyQualifiedName~CriticalLoanCreationTests"` | 10 aprovados, 0 falhas, 0 ignorados |
| `dotnet test backend/backend.sln --no-build --no-restore --artifacts-path .tmp/quality/T015/backend -c Release --nologo --disable-build-servers` | 172 aprovados, 0 falhas, 0 ignorados |
| `git diff --check` | código 0; avisos informativos de LF/CRLF preexistentes |

O workflow `.github/workflows/container-ci.yml` já executa em pull requests para `develop` nos eventos `opened`, `synchronize` e `reopened`, com jobs independentes de qualidade e testes backend. A restauração do job existente ainda não usa `--locked-mode`; essa lacuna de reprodutibilidade está fora da posse e do escopo da T015.

## Arquivos alterados

- `backend/LabSolos-Server-DotNet8/Controllers/EmprestimosController.cs`
- `backend/Tests/Integration/CriticalLoanDecisionTests.cs`
- `.codex/docs/specs/fundacao-qualidade-testes/evidencias/T015.md`

Não foram alterados `tasks.md`, outros arquivos de produção ou caminhos fora da posse. Não houve commit, push, merge, comentário externo, fechamento da issue ou início de outra tarefa.

## Falhas e riscos preexistentes

- Permanecem avisos históricos de análise .NET (`CA*`, incluindo `CA1305`, `CA1707`, `CA1848` e outros) fora do escopo da T015.
- `git diff --check` emite apenas avisos de conversão LF/CRLF dos arquivos modificados e de alterações preexistentes no worktree.
- O contrato de concorrência entre solicitações não foi ampliado; a T015 cobre reprocessamento sequencial conforme o plano.
- A inconsistência de devolução relacionada a `DataDevolucao` permanece deliberadamente para a T016/QC-001.
