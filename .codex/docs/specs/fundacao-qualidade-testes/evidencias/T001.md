# Evidência T001 — Mapear a cobertura existente e as lacunas do aceite

**Issue:** #227
**Data:** 2026-09-14
**Revisão:** `a58cc224c2e6a327434674a0de0585adc62219a2`
**Base:** `origin/develop` no mesmo SHA
**Status do Project ao iniciar e ao finalizar:** `In Progress`
**Responsável persistido:** `nathannmvr`

## Escopo e sincronização

- A issue foi confirmada em `ifpebj-ti/lab-solos` e está aberta.
- O Project configurado foi resolvido por `PROJECT_OWNER=ifpebj-ti` e `PROJECT_NUMBER=41`.
- O item já estava atribuído a `nathannmvr`; o responsável existente foi preservado.
- O item PVTI correspondente foi alterado de `Done` para `In Progress` e verificado novamente pelo Project.
- Não foram adicionados comentários, a issue não foi fechada e nenhum outro campo externo foi alterado.
- Como a issue possui outras tarefas pendentes, o status final permanece `In Progress`.

## Estado inicial do checkout

O checkout estava sujo antes da documentação, com alterações preexistentes em artefatos `backend/Tests/bin`/`obj` e `frontend/tsconfig.app.tsbuildinfo`, além de `tasks.md`, `techspec.md` e `.tmp/` não rastreados. A restauração/descoberta continuou usando `--artifacts-path .tmp/quality/T001/backend` para não disputar os artefatos existentes.

Não havia alteração de código-fonte de aplicação, testes, workflow, Compose ou configuração E2E relevante para esta tarefa. A verificação de nomes sob esses caminhos retornou somente os artefatos de build já modificados; portanto, a evidência da techspec foi reutilizada onde os arquivos relevantes permaneceram no mesmo SHA.

## RED — verificação documental inicial

A T001 é documentação e descoberta, não mudança de comportamento de produção. O RED foi feito por uma verificação objetiva inline que exige a matriz e seus identificadores antes de aceitá-la:

```powershell
$path='.codex/docs/specs/fundacao-qualidade-testes/cobertura.md'
if (!(Test-Path $path)) {
  Write-Output 'RED: cobertura.md inexistente; matriz cenários→testes/evidências ainda não está disponível.'
  exit 1
}
```

Resultado inicial: **exit 1**, com `RED: cobertura.md inexistente; matriz cenários→testes/evidências ainda não está disponível.` Isso demonstra a lacuna documental prevista, sem fabricar falha de produção.

## GREEN — matriz preenchida

Foram criados somente:

- `.codex/docs/specs/fundacao-qualidade-testes/cobertura.md`
- `.codex/docs/specs/fundacao-qualidade-testes/evidencias/T001.md`

O primeiro conteúdo verde vinculou CT-001–CT-009 a caminhos e nomes de testes, separou unidade, HTTP/PostgreSQL, UI simulada e E2E real, registrou positivo/negativo, comando de descoberta, lacuna e tarefa de seguimento, identificou as quatro unidades históricas de `ProdutoRepository` e registrou QC-001–QC-006 como hipóteses não confirmadas.

## REFACTOR — organização documental

Após a verificação verde, as referências repetidas foram consolidadas em uma legenda de camadas, uma matriz única CT-001–CT-009, uma seção de descoberta e uma tabela de candidatos QC. A organização foi ajustada para deixar explícito que:

- `route.fulfill` é UI simulada, não E2E integrado;
- ausência de `route.fulfill` não comprova isolamento;
- testes de controlador não comprovam middleware HTTP;
- contagem de descoberta não equivale a cobertura crítica;
- hipóteses QC não são achados confirmados.

Não houve alteração de código ou comportamento. A verificação objetiva foi repetida após a organização e passou.

## Comandos e resultados

| Comando | Resultado observado |
|---|---|
| `dotnet sln backend/backend.sln list` | **0** — listou `LabSolos-Server-DotNet8/LabSolos-Server-DotNet8.csproj` e `Tests/Tests.csproj`. |
| `dotnet test backend/Tests/Tests.csproj --artifacts-path .tmp/quality/T001/backend --list-tests --nologo` | **0** — descoberta não vazia; a saída listou 125 casos. A execução com restauração inicial reportou somente os avisos preexistentes `CS8604` nas linhas de `DateTime.Parse` de `ProdutoMappingProfile.cs`. |
| `npm run test:e2e:list` em `frontend` | **0** — `Total: 201 tests in 7 files`; somente o projeto `chromium` foi descoberto. |
| `git diff --check` | **0** — nenhuma falha de whitespace. |
| Verificação documental objetiva final (CT-001–CT-009, QC-001–QC-006, Positivo, Negativo, Descoberta, Lacuna) | **0** — matriz documental completa. |

O comando auxiliar de contagem da lista backend, executado com `--no-restore` e saída em `.tmp/quality/T001/backend-list-tests.txt`, confirmou `test_cases=125` e `exit=0`. Os artefatos `.tmp/quality/T001/**` são saídas de validação e não fazem parte dos arquivos alterados.

## Ambiente e diferenças local/CI

| Entrada | Local observado | CI/workflow observado | Diferença relevante |
|---|---|---|---|
| Sistema | Windows | `ubuntu-latest` | SO, shell e imagens do runner diferem. |
| Node/npm | Node `v24.14.0`, npm `11.9.0` | Node `20`; npm não fixado separadamente | A suíte local não comprova equivalência de runtime. |
| .NET | SDK `8.0.419` | `8.0.x` | Patch não é fixado no workflow. |
| Python | `3.14.3` | `3.12` no `container-ci.yml` | Versões distintas. |
| Docker | Engine `29.7.2` | Engine provido pelo runner | Versão local não é a do CI. |
| PostgreSQL | Fixture Testcontainers documentada como `postgres:16-alpine`; Compose E2E usa `postgres:15-alpine` | Compose E2E também usa `postgres:15-alpine` | Existem dois majors por ambiente de teste; não foram alinhados nesta tarefa. |
| Dependências | `frontend/package-lock.json` existe; não há `global.json`, `.node-version`, `packageManager` ou `packages.lock.json` NuGet | `npm ci` usa o lock npm; restore .NET atual não usa `--locked-mode` | RNF-001 permanece lacuna planejada para T002–T004. |
| Playwright | `@playwright/test` `1.62.1`; 201 casos descobertos | Instala Chromium pelo lock npm e executa `npm run test:e2e` | A execução desta tarefa foi somente descoberta, não E2E. |

O CI atual dispara `container-ci.yml` para `pull_request` em `develop` nos eventos `opened`, `synchronize` e `reopened`, além de `workflow_dispatch`. Ele ainda não cobre `edited`, `ready_for_review` ou `merge_group`; a comprovação de proteção de branch é posterior e não faz parte de T001.

## Falhas preexistentes e limites

- Os dois avisos `CS8604` de `ProdutoMappingProfile.cs` foram observados durante a restauração/compilação implícita da descoberta; não foram corrigidos porque T001 só documenta e o impacto é objeto de QC-004/T017.
- O checkout sujo e os artefatos `bin/obj`/tsbuildinfo são preexistentes; a T001 não os reverteu nem os incluiu na documentação.
- Não foi executada a suíte completa backend, Vitest ou E2E; a tarefa exige descoberta, não uma nova execução integral. A techspec registra as execuções amplas anteriores e suas limitações.
- A lista de 201 casos Playwright contém testes simulados, contratos reais pontuais e um caso real stateful; zero casos descobertos não foi usado como cobertura.
- Nenhum dos seis candidatos QC foi promovido a defeito confirmado nesta tarefa.

## Confirmação de escopo

`tasks.md` **não foi editado**. Não houve commit, push, merge, fechamento de issue, início de outra tarefa ou alteração fora dos dois caminhos sob responsabilidade da T001.
