# Evidência T014 — criação de empréstimo com sessão e persistência reais

- Data: 2026-09-14
- Issue: #227
- Project 41: `In Progress`, atribuída a `nathannmvr`
- Dependência: T010 concluída
- Resultado: concluída como cobertura/caracterização; não houve divergência de produção
- Ambiente observado: Windows, SDK .NET 8.0.419, Docker Engine 29.7.2

## Escopo verificado

Os testes HTTP usam `IntegrationWebApplicationFactory`, PostgreSQL/Testcontainers e login real para verificar que:

- usuário habilitado cria empréstimo com `201 Created`;
- a identidade do solicitante vem da sessão, mesmo com `solicitanteId` conflitante enviado no corpo;
- resposta e consulta posterior contêm o empréstimo, solicitante e produto/quantidade persistidos;
- usuário anônimo é recusado com `401` e não cria registro;
- sessão de administrador mantém o `401` legado documentado e não cria registro.

## RED

Antes da cobertura, a linha de base direcionada foi executada:

```text
dotnet test backend/Tests/Tests.csproj --filter FullyQualifiedName~CriticalLoanCreationTests --nologo --disable-build-servers
```

O comando terminou com código 0, mas informou que nenhum teste correspondia ao filtro. Após a criação dos testes, o comportamento existente passou integralmente; isso foi tratado como caracterização, conforme a exceção TDD prevista na skill, sem fabricar uma falha de produção.

## GREEN

- Restore bloqueado: passou.
- Testes direcionados: 3 aprovados, 0 falhas, 0 ignorados.
- A implementação existente atendeu aos contratos de sessão, resposta, persistência e recusas; `EmprestimosController.cs` não precisou de alteração.

## REFACTOR

- Relógio controlado reutilizado na preparação do cenário e na aplicação.
- Asserções de resposta e consulta persistida consolidadas em helper compartilhado.
- Preparação PostgreSQL, autenticação e contagem de registros permaneceram isoladas por cenário.
- Os 3 testes direcionados continuaram verdes.

## Validações

| Comando | Resultado |
|---|---|
| `dotnet restore backend/backend.sln --locked-mode --artifacts-path .tmp/quality/T014/backend` | código 0 |
| `dotnet test backend/Tests/Tests.csproj --no-restore --artifacts-path .tmp/quality/T014/backend -c Release --nologo --disable-build-servers --filter "FullyQualifiedName~CriticalLoanCreationTests"` | 3 aprovados, 0 falhas, 0 ignorados |
| `dotnet test backend/backend.sln --no-build --no-restore --artifacts-path .tmp/quality/T014/backend -c Release --nologo --disable-build-servers` | 162 aprovados, 0 falhas, 0 ignorados |
| `git diff --check` | código 0 |

## Arquivos e limites

- Alterado: `backend/Tests/Integration/CriticalLoanCreationTests.cs`
- Alterado: `.codex/docs/specs/fundacao-qualidade-testes/evidencias/T014.md`
- Não alterado: `backend/LabSolos-Server-DotNet8/Controllers/EmprestimosController.cs`
- `tasks.md` não foi editado.
- Não houve commit, push, merge, comentário externo, fechamento de issue ou início de outra tarefa.

## Preexistências e riscos

Os builds continuam emitindo avisos históricos de análise .NET em arquivos fora da posse T014, incluindo regras CA e avisos `CS8604`; os avisos de LF/CRLF do `git diff --check` também são informativos e preexistentes. A tarefa não antecipou a correção de `DataDevolucao`, reservada às tarefas sequenciais T015/T016.
