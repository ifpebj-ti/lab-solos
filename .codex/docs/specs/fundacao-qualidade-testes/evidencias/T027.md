# Evidência T027 — inventário, baseline e impedimentos

- Data: 2026-09-15
- Tarefa: T027
- Resultado: concluída
- Issue: #237 (aberta; Project 41 em `In Progress`)

## RED

A primeira tentativa de consolidação encontrou somente o snapshot temporário:
o inventário, baseline, pendências e evidência ainda não existiam. A coleta de
T027, entretanto, terminou com código 0 no toolchain isolado e produziu
`.tmp/quality/T027/current.json`.

## GREEN

O snapshot contém 4 produtores bem-sucedidos e 257 achados: 253 médios, 1
baixo e 3 altos. Os seis achados funcionais QC-001–QC-006 foram preservados;
QC-006 foi triado com evidência objetiva de build e não foi elevado
automaticamente por causa de um pacote grande.

O baseline em `.github/quality/baseline.json` contém 254 ocorrências
históricas média/baixa, cada uma com `backlogId` rastreável por fingerprint e
`owner`. Os três altos QC-002, QC-003 e QC-005 ficaram fora do baseline e
continuam no inventário como impedimentos. `pendencias.md` lista as 254
ocorrências individualmente.

## REFACTOR e validações

- `python -B -m unittest discover -s .github/scripts/tests -p test_quality_baseline.py -v`:
  25 aprovados.
- Regressão Python completa: 309 aprovados, 2 skips preexistentes.
- `python -B .github/scripts/quality_baseline.py collect --output .tmp/quality/T027/current.json`:
  código 0, 4 produtores e 257 achados.
- `python -B .github/scripts/quality_baseline.py render --current .tmp/quality/T027/current.json --output .codex/docs/specs/fundacao-qualidade-testes/inventario.md`:
  código 0.
- `python -B .github/scripts/quality_baseline.py check --current .tmp/quality/T027/current.json --base-ref origin/develop`:
  código 2 no host, porque o bootstrap da revisão base falhou na divergência
  do toolchain local; o comando não faz fetch nem aceita a cópia local como
  base confiável. Os contratos controlados de T009 comprovam código 1 para
  achado novo/alto aberto e código 2 para configuração incompleta.
- `git diff --check`: código 0; os avisos exibidos são apenas conversões de
  final de linha do Git.

## Limites e cobertura

`cobertura.md` liga CT-001–CT-008 às evidências reais de T024–T026 e mantém
CT-009/QC-001 como lacuna explícita. A descoberta final registra 211 testes
Playwright em 10 arquivos, 14 no projeto `real` e 197 no projeto `ui`.

T027 consolida o estado e não declara CA-002 verde: QC-002, QC-003 e QC-005
continuam altos e abertos. Não houve commit, push, merge, PR, fechamento de
issue ou alteração de `tasks.md` pelo agente.
