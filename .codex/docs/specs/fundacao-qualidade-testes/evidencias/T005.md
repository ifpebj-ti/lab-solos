# Evidência T005 — Diagnósticos backend com analisadores e SARIF por projeto

- Tarefa: T005
- Issue: #237
- Data: 2026-09-14
- Revisão observada: `a58cc224c2e6a327434674a0de0585adc62219a2`
- Ambiente: Windows, .NET SDK `8.0.419`, Python `3.14.3`
- Project: item presente no projeto 41 de `ifpebj-ti`, status `In Progress`, assignee `nathannmvr`

## Escopo e sincronização

A Issue #237 foi confirmada em `ifpebj-ti/lab-solos` e permaneceu aberta. Antes
do RED, o usuário autenticado `nathannmvr`, `PROJECT_OWNER=ifpebj-ti` e
`PROJECT_NUMBER=41` foram consultados. O item já estava no Project, atribuído ao
usuário autenticado e com status `In Progress`; a persistência foi verificada
novamente antes da implementação. Como existem tarefas posteriores para a
mesma issue, o status final permanece `In Progress`.

O caminho solicitado para a skill (`D:\lab-solos\skills\execute-task\SKILL.md`)
não existe neste checkout; foi usada a cópia equivalente em
`.codex/skills/execute-task/SKILL.md`, lida integralmente antes da execução.

`tasks.md` permaneceu sob controle do orquestrador e não foi editado.

## Estado inicial

As dependências estavam atendidas: T003 constava como concluída. O checkout
continha alterações locais de tarefas anteriores, incluindo
`backend/Directory.Build.props`, artefatos de compilação e documentação; elas
foram preservadas. Antes da T005 não existiam `Directory.Build.targets`,
`backend/.editorconfig`, o teste contratual, as fixtures de análise nem a
evidência desta tarefa. O `.editorconfig` específico do projeto já continha a
exceção `CS8618 = silent` e não foi sobrescrito.

Linha de base direcionada, antes de criar o teste:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_dotnet_analysis.py -v
Ran 0 tests in 0.000s
NO TESTS RAN
```

## RED

Foram criados primeiro o teste contratual e fixtures isoladas, sem configurar a
produção. A execução falhou pelas lacunas esperadas:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_dotnet_analysis.py -v
Ran 2 tests ...
FAILED (failures=2)
```

As falhas demonstraram `AnalysisLevel` ausente e lista de relatórios SARIF
vazia (`[]`). Não foi usada falha acidental de compilação como RED.

## GREEN

Foi implementado o mínimo necessário:

- `backend/Directory.Build.props` fixa `AnalysisLevel=8.0`,
  `AnalysisMode=Recommended`, habilita os analisadores .NET e mantém a análise
  durante o build; as propriedades usam condição para respeitar valores
  explícitos do projeto.
- `backend/Directory.Build.targets` define `ErrorLog` em SARIF 2.1, com arquivo
  sob o `IntermediateOutputPath` de cada projeto, cria o diretório e remove o
  relatório anterior antes do `CoreCompile`.
- `backend/.editorconfig` torna `CA1502` explicitamente `warning`, sem
  transformar os avisos existentes em erros.
- A nulidade dos projetos permaneceu habilitada; o `.editorconfig` específico
  de `LabSolos-Server-DotNet8` continuou preservando `CS8618 = silent`.
- As fixtures cobrem uma aplicação com complexidade conhecida e nulidade, e um
  projeto de testes sem `CA1502`.

O contrato direcionado passou após a correção da expectativa de ordenação dos
nomes dos arquivos:

```text
Ran 2 tests in 6.107s
OK
```

O teste confirmou `CA1502` na aplicação, ausência dessa regra no projeto de
testes, SARIF 2.1.0 e dois caminhos de saída distintos.

## REFACTOR

O contrato foi fortalecido para copiar a configuração específica do projeto e
verificar a precedência: `CS8618` não aparece, enquanto `CS8602` continua sendo
emitido. Também passou a confirmar que os diretórios dos relatórios não são o
mesmo caminho. Após o REFACTOR, a mesma suíte permaneceu verde com 2 testes
aprovados.

No rebuild real, o relatório antigo da aplicação foi removido/regenerado: após
substituir a fixture complexa por uma implementação simples, `CA1502` deixou de
aparecer no mesmo arquivo SARIF.

## Validação oficial

| Comando | Resultado |
|---|---|
| `python -B -m unittest discover -s .github/scripts/tests -p test_quality_dotnet_analysis.py -v` | **0** — 2 testes aprovados após REFACTOR |
| `dotnet restore backend/backend.sln --locked-mode --artifacts-path .tmp/quality/T005/backend` | **0** — todos os projetos atualizados para restauração bloqueada |
| `dotnet build backend/backend.sln --no-restore --artifacts-path .tmp/quality/T005/backend -c Release -t:Rebuild --nologo --disable-build-servers` | **0** — rebuild da aplicação e dos testes, 0 erros |
| `dotnet test backend/backend.sln --no-build --no-restore --artifacts-path .tmp/quality/T005/backend -c Release --nologo --disable-build-servers` | **0** — 125 aprovados, 0 falhas, 0 ignorados |
| `git diff --check` | **0** — somente avisos preexistentes de conversão LF/CRLF em `frontend/package.json` e `frontend/vite.config.ts` |

Os relatórios reais produzidos pelo rebuild foram:

```text
.tmp/quality/T005/backend/obj/LabSolos-Server-DotNet8/release/quality/LabSolos-Server-DotNet8.sarif  version=2.1.0  results=141
.tmp/quality/T005/backend/obj/Tests/release/quality/Tests.sarif                                     version=2.1.0  results=84
```

## Falhas preexistentes, limitações e riscos

- Os dois avisos `CS8604` em `Mappings/ProdutoMappingProfile.cs`, linhas 48 e
  50, permaneceram e correspondem ao QC-004/T017; não foram suprimidos nem
  corrigidos nesta tarefa.
- A análise recomendada passou a revelar avisos históricos de manutenção e
  confiabilidade no código de produção e nos testes. Isso é diagnóstico para
  T007/T008/T009, não motivo para excluir regras nesta tarefa.
- Uma execução do rebuild registrou avisos transitórios `MSB3061` porque o
  `testhost` ainda mantinha arquivos do diretório de artefatos abertos; o
  processo terminou, a execução seguinte de teste passou e o rebuild concluiu
  com código 0.
- Não foram executados coletores reais, comparação de baseline ou gate de
  severidade; essas responsabilidades pertencem às tarefas posteriores.

Não houve bloqueio. Não houve commit, push, merge, fechamento da issue ou início
de outra tarefa. `tasks.md` não foi editado.
