# Evidência T007 — Normalização e identidade estável dos achados

- Tarefa: T007
- Issue: #237
- Data: 2026-09-14
- Revisão observada: `a58cc224c2e6a327434674a0de0585adc62219a2`
- Ambiente: Windows 10 (`10.0.19045.0`), Python `3.14.3`
- Status do Project 41: `In Progress`

## Sincronização da issue

Antes do RED, foi confirmado que a issue #237 pertence a `ifpebj-ti/lab-solos`, está aberta e está atribuída a `nathannmvr`. O item existente no Project 41 (`ifpebj-ti`) foi atualizado de `Todo` para `In Progress` e a persistência foi confirmada por nova consulta. Como há tarefas posteriores para a mesma issue, o status não foi alterado para `Done`.

## Linha de base e RED

Antes da implementação, o teste direcionado foi executado:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_baseline.py -v
```

Resultado da linha de base: `Ran 0 tests` / `NO TESTS RAN`, pois o teste da T007 ainda não existia.

Após criar o teste e as fixtures, mas antes de criar o módulo de produção, o RED esperado ocorreu:

```text
ImportError: Failed to import test module: test_quality_baseline
ModuleNotFoundError: No module named 'quality_baseline'
```

Esse RED demonstra a ausência da normalização, e não uma falha acidental de ambiente.

## GREEN

Foi implementado o núcleo puro de `.github/scripts/quality_baseline.py`:

- modelo versionado de `current.json`, com metadados exigidos e versão do algoritmo;
- validação de `policy.json` sem criar ou aprovar um baseline vazio;
- normalização de caminhos relativos, separadores Windows/Linux e URIs SARIF;
- classificação por regra e módulo, usando o prefixo mais específico;
- fingerprint SHA-256 versionado com ferramenta, regra, caminho relativo e contexto normalizado;
- identidade independente de linha, SHA, data e prefixo absoluto da máquina;
- adaptação de JSON ESLint e SARIF 2.1.0;
- preservação de multiplicidade, evidência, estado, `backlogId` e `owner`;
- rejeição explícita de regra desconhecida, módulo ausente, relatório malformado e configuração inválida.

Comando e resultado:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_baseline.py -v
Ran 12 tests ...
OK
```

As fixtures cobrem deslocamento de linha, separadores, caminho absoluto, colisão de contexto, duplicatas, remoção/inclusão, regra desconhecida, caminho sem módulo, relatório malformado, SARIF e metadados preservados.

## REFACTOR

Após o GREEN, a leitura da localização SARIF foi extraída para um helper comum, com validação segura de `region` e `snippet`. A normalização de contexto foi consolidada para whitespace/pontuação insignificante. Durante a revisão final, foi corrigido o tratamento do prefixo `.github`, preservando o ponto inicial necessário para classificar automação.

O mesmo teste direcionado foi reexecutado após a refatoração e permaneceu verde com 12 casos.

## Validação final

Comandos especificados pela T007:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_baseline.py -v
```

Resultado: 12 aprovados, zero falhas.

```text
git diff --check
```

Resultado: código 0; nenhum erro de whitespace. O Git exibiu apenas avisos preexistentes de conversão LF/CRLF em arquivos frontend fora da posse da T007.

Regressão proporcional:

```text
python -B -m unittest discover -s .github/scripts/tests -p 'test_*.py' -q
```

Resultado: `Ran 271 tests`, `OK (skipped=2)`. Não houve falhas preexistentes ou regressões; os dois skips pertencem ao conjunto existente e permanecem registrados para validação em Linux conforme a esteira do projeto.

## Limitações e escopo

T007 não executa ESLint/SARIF reais, não coleta o inventário e não implementa `collect`, `check` ou `render`; essas responsabilidades pertencem às tarefas posteriores. Nenhum `baseline.json` vazio foi criado ou tratado como aprovado.

Foram alterados somente os caminhos sob responsabilidade da T007. `tasks.md` não foi editado; o registro/log da tarefa permanece sob controle do orquestrador.
