# Evidência T002 — Fixar o contrato de versões e validar o ambiente

- Tarefa: T002
- Issue: #227
- Project: item no projeto `LabOn — Desenvolvimento e Qualidade`, status `In Progress`, assignee `nathannmvr`
- Data: 2026-09-14
- Escopo: manifesto de toolchain, dependência Python, verificador e testes de contrato

## Contrato entregue

O manifesto `.github/quality/toolchain.json` registra versões completas, comandos
sem shell, fontes de resolução e a separação entre versão exigida e revisão
observada do runner:

| Componente | Versão/revisão exigida ou observada |
|---|---|
| Node.js | `20.20.2` |
| npm | `10.8.2` |
| .NET SDK | `8.0.419`, compatível com `net8.0` |
| Python | `3.12.14` |
| Docker Engine | `29.7.2` |
| Docker Compose | `5.3.1` |
| Runner | Ubuntu 24.04 x64; revisão observada `20260907.300.1`, commit da imagem `fc63e1b4dbfacf7e2449bf0706226f9f6eea583e` |
| Playwright | pacote `1.62.1`; Chromium/Chrome for Testing `151.0.7922.34`, revisão `1234` |

Node 20.20.2 e npm 10.8.2 foram conferidos no índice oficial de distribuições;
Python 3.12.14 foi conferido na página oficial da release. A revisão do runner
foi obtida da release Ubuntu 24.04 de 2026-09-07. O Chromium foi lido do
`playwright-core/browsers.json` associado ao pacote `1.62.1` instalado a partir
do lockfile `frontend/package-lock.json`.

`requirements.txt` fixa `PyYAML==6.0.3`, dependência Python já utilizada pela
esteira. O verificador executa listas de argumentos, suporta o wrapper `npm.cmd`
no Windows, interpreta prefixos/sufixos de versão e não imprime stdout/stderr de
ferramentas em mensagens de erro.

## RED

Antes da implementação, o comando direcionado encontrou zero testes:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_toolchain.py -v
Ran 0 tests in 0.000s
NO TESTS RAN
```

Depois que o teste foi criado, a primeira execução falhou pela capacidade ainda
ausente, como esperado:

```text
ImportError: Failed to import test module: test_quality_toolchain
ModuleNotFoundError: No module named 'check_quality_toolchain'
```

Essa falha não foi causada por ambiente quebrado: o executor unittest iniciou e
carregou o arquivo de teste; faltava somente o módulo previsto pela tarefa.

## GREEN

Foi implementado o menor contrato executável para carregar/validar o JSON,
consultar cada ferramenta, interpretar versões `X.Y.Z`, distinguir divergência
de versão de falha operacional e proteger mensagens contra vazamento de saída.

Após corrigir um cálculo de raiz no próprio teste, a suíte passou:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_toolchain.py -v
Ran 10 tests in 0.007s
OK
```

## REFACTOR

Após GREEN, o verificador foi refatorado para:

- separar erro operacional de incompatibilidade, mantendo códigos `0`, `1` e `2`;
- usar o parser declarado no manifesto;
- resolver executáveis por caminho sem shell, incluindo `npm.cmd` no Windows;
- substituir importação dinâmica por importação explícita de `sys`.

A regra de compatibilidade do wrapper recebeu teste próprio e a suíte final passou
novamente:

```text
python -B -m unittest discover -s .github/scripts/tests -p test_quality_toolchain.py -v
Ran 11 tests in 0.007s
OK
```

## Validação da tarefa

| Comando | Resultado |
|---|---|
| `python -B -m unittest discover -s .github/scripts/tests -p test_quality_toolchain.py -v` | 11 aprovados |
| `python -B .github/scripts/check_quality_toolchain.py --help` | exit `0` |
| `python -B .github/scripts/check_quality_toolchain.py` | exit `1`, divergência ambiental acionável |
| `git diff --check` | exit `0` |
| `python -B -m unittest discover -s .github/scripts/tests -p 'test_*.py' -v` | 251 aprovados, 2 skips |

O comando real produziu somente diagnósticos de versão, sem credenciais:

```text
ERROR: node: versão incompatível; requerida 20.20.2, observada 24.14.0
ERROR: npm: versão incompatível; requerida 10.8.2, observada 11.9.0
ERROR: python: versão incompatível; requerida 3.12.14, observada 3.14.3
```

O .NET SDK, Docker Engine e Docker Compose disponíveis nesta máquina
correspondem aos valores registrados. Node/Python permanecem em majors locais
mais novos que as linhas CI exigidas; nenhuma instalação global ou migração de
major foi feita. A combinação pinada foi exercitada com saídas sintéticas nos
testes e o checker reporta corretamente incompatibilidade quando executado no
host divergente.

Os dois skips da regressão completa são preexistentes e decorrem da ausência de
privilégio para criar symlink no Windows. Não houve falha preexistente reproduzida
na suíte Python.

## Escopo e estado externo

Arquivos sob posse alterados:

- `.github/quality/toolchain.json`
- `.github/quality/requirements.txt`
- `.github/scripts/check_quality_toolchain.py`
- `.github/scripts/tests/test_quality_toolchain.py`
- `.codex/docs/specs/fundacao-qualidade-testes/evidencias/T002.md`

`tasks.md` não foi editado. Nenhum arquivo fora dos caminhos sob responsabilidade
foi alterado por esta tarefa. Não houve commit, push, merge ou fechamento da
issue; o item do Project permanece `In Progress` porque a issue contém trabalho
posterior no plano.
