# Evidência T012 — Listas do mentor

- Issue: #231 — aberta, atribuída a `nathannmvr`, Project 41 em `In Progress`.
- Escopo: `mentor/MyClass`, `mentor/Disabled`, `mentor/HistoryClass`, testes React, fixtures e cenários T012 da suíte responsiva.
- `tasks.md` fica sob atualização do orquestrador.

## RED

Os testes de contrato foram criados para as três listas. O baseline legado usava `HeaderTable.columns`, linhas sem rótulos `dt`/`dd` e contêineres com largura mínima incompatível com os breakpoints móveis. As expectativas cobriram nomes e e-mails longos, status, filtros, paginação e o destino do histórico da turma.

## GREEN

As três páginas agora usam `ResponsiveTable` com colunas rotuladas, preservando filtros, ordenação, paginação, contadores, ações e destinos existentes. Os contêineres usam `min-w-0` e largura útil a partir de `md`; as células aceitam conteúdo longo com altura automática. O histórico da turma é validado na rota `/mentor/history/class`.

```text
npm --prefix frontend run test -- --run src/pages/mentor/MyClass.responsive.test.tsx src/pages/mentor/Disabled.responsive.test.tsx src/pages/mentor/HistoryClass.responsive.test.tsx --maxWorkers=1
3 arquivos, 3 testes aprovados
```

## REFACTOR e qualidade

```text
npm --prefix frontend run test -- --run
52 arquivos, 200 testes aprovados

npm --prefix frontend run lint
aprovado, sem warnings ESLint

npm --prefix frontend run build
aprovado; somente avisos preexistentes de `/env.js`, imagem não resolvida, Browserslist e chunks grandes

$env:E2E_SMTP_PORT='28025'; docker compose -f docker-compose-e2e.yml up -d --build --wait
npm --prefix frontend run test:e2e -- e2e/responsive-layout.spec.ts --grep T012 --workers=2
15/15 cenários aprovados em 9,5 s
```

A matriz E2E cobre minha turma, mentorados desativados e histórico da turma nos viewports 320×800, 375×812, 767×900, 768×1024 e 1440×900. Foram verificados dados longos, labels mobile, alinhamento desktop, rota do histórico, ausência de overflow e estados de filtros. As capturas 320/1440 de minha turma foram revisadas. A primeira rodada do histórico apontou a rota incorreta usada no teste; ela foi corrigida para `/mentor/history/class` e os 15 cenários foram repetidos com sucesso. `frontend/tsconfig.app.tsbuildinfo` foi restaurado após o build.

## Limitações

Não há dispositivo físico ou leitor de tela neste ambiente; roles, labels, foco e teclado foram verificados em RTL/Playwright. A suíte completa imprime stacks esperados nos testes de contratos inválidos, mas encerra com todos os 200 testes aprovados.
