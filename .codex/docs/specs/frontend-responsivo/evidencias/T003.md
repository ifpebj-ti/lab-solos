# Evidência T003 — Migrar aprovação e recusa de cadastros

- Issue: #231 (`ifpebj-ti/lab-solos`)
- Project: `ifpebj-ti` / 41, item `PVTI_lADOCHXT-84BDe_Fzg32NF8`
- Estado remoto final: issue aberta, `nathannmvr` atribuído e item em `In Progress` (mantido porque T004–T016 ainda pertencem à issue)
- Rota/perfil: `/admin/register-request`, Administrador

## RED

1. A caracterização inicial passou com 2 testes: os callbacks de recusa e aprovação continuavam recebendo os IDs 11 e 22 exatamente uma vez.
2. Após acrescentar as asserções do requisito ausente, o comando abaixo terminou com 2 testes verdes e 2 falhas esperadas:

   `npm --prefix frontend run test -- --run src/components/global/table/ItemButton.test.tsx src/pages/RegistrationRequests.responsive.test.tsx --maxWorkers=1 --pool=threads`

   - `ItemButton`: não existia `listitem`, associação `dt`/`dd` ou nome acessível por ação e registro.
   - `RegistrationRequests`: não existia lista acessível chamada `Solicitações de cadastro`; a tela ainda usava largura mínima de 800 px.

O cenário Playwright T003 foi escrito na fase RED. A falha automatizada executada e registrada veio dos testes RTL acima; a primeira execução do navegador ocorreu após o GREEN e não é apresentada como RED.

## GREEN

- `ItemButton` passou a adaptar dados e ações ao `ResponsiveTable`, com uma única árvore, célula de ações rotulada, botões nativos `type="button"`, nomes que incluem a pessoa, foco visível e alvo de 44 × 44 px em mobile/ponteiro grosseiro.
- `RegistrationRequests` passou a compartilhar uma definição de cinco colunas entre cabeçalho e linhas, usar chave estável do registro e remover o contêiner de 800 px/rolagem lateral. Ferramentas, título, estados e paginação permanecem no fluxo e os callbacks continuam atualizando a lista.
- Vitest direcionado final: 2 arquivos, 5 testes aprovados.
- Regressão proporcional: `userListConsumers`, `ResponsiveTable`, `ItemClickable`, `ItemButton` e `RegistrationRequests`, 5 arquivos e 17 testes aprovados antes do último acréscimo de cobertura; o direcionado final validou a cobertura acrescentada.
- Playwright direcionado final: 5/5 em Chromium, `320×800`, `375×812`, `767×900`, `768×1024` e `1440×900`, com `--workers=2`.
- E2E validou overflow do documento/registro/células, rótulos mobile, alinhamento desktop, conteúdo de 200 caracteres, instituição ausente, sete itens e segunda página, nomes e teclado das duas ações, alvo touch e busca/foco preservados em `375 → 768 → 375` sem nova consulta.

## REFACTOR e qualidade

- Ações e classes de interação foram consolidadas em `ItemButton`; mapeamento de colunas ficou estável e duplicações de largura foram removidas.
- Detector Impeccable: `[]` para `ItemButton.tsx` e `RegistrationRequests.tsx`.
- `npm --prefix frontend run lint`: aprovado, zero avisos.
- `npm --prefix frontend run build`: aprovado.
- `npm --prefix frontend run test:e2e:list`: 46 testes descobertos, incluindo cinco T003.
- `docker compose -f docker-compose-e2e.yml up -d --build --wait`: imagens reconstruídas e backend, banco, SMTP e frontend saudáveis.
- Screenshots auxiliares inspecionadas em `frontend/e2e/infra/artifacts/test-results`: `solicitacoes-320.png` e `solicitacoes-1440.png`; cards mobile e grade desktop conservaram os mesmos dados e ações.

## Lacunas e observações

- A inspeção foi feita em Chromium emulado. Não havia dispositivo físico nem leitor de tela para validação complementar; semântica, foco e teclado foram cobertos automaticamente.
- O build manteve avisos preexistentes: `env.js` sem `type="module"`, imagem `laboratory.png` resolvida em runtime, base Browserslist antiga e chunk principal acima de 500 kB.
- Playwright informou apenas que `NO_COLOR` foi ignorado porque `FORCE_COLOR` estava definido.
- O workflow `container-ci.yml` executa `frontend-quality` e `auth-e2e` em PR para `develop` nos eventos `opened`, `synchronize` e `reopened`. O endpoint clássico de branch protection retorna 404, mas a API de rulesets confirma checks obrigatórios em `develop`; `Authentication E2E` não está entre eles. A lacuna segue para T016.
- Nenhuma issue foi fechada e nenhum comentário, commit, push ou PR foi criado.
