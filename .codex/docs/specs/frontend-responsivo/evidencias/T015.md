# Evidência T015 — Encerramento da compatibilidade legada

- Issue: #231 — aberta, atribuída a `nathannmvr`, Project 41 em `In Progress`.
- Escopo: primitivas de tabela, contexto de colunas, testes de contrato e regressão E2E.
- `tasks.md` fica sob atualização do orquestrador.

## RED

A auditoria inicial encontrou ramos de fallback e props `columnWidths` em `Header`, `Item`, `ItemOnly`, `ItemClickable`, `ItemButton`, `ItemButtonLink`, `ItemDelete`, `ItemReturn` e `TableItemWithActions`. Os testes legados foram convertidos para caracterizar a exigência de contexto, correspondência de colunas e ações rotuladas.

## GREEN

As primitivas agora exigem `ResponsiveTable` e colunas únicas, rotuladas e ponderadas; não há fallback silencioso para markup sem `dt`/`dd`. Consumidores de produção não usam mais `columnWidths`; as únicas ocorrências restantes pertencem aos documentos PDF independentes de exportação (`LoanDoc` e `MyDocument`). A proteção de ref em `ResponsiveRecord` continua compatível com os gatilhos Radix.

```text
npm --prefix frontend run test -- --run src/components/global/table --maxWorkers=1
12 arquivos, 30 testes aprovados
```

## REFACTOR e qualidade

```text
npm --prefix frontend run test -- --run
56 arquivos, 204 testes aprovados

npm --prefix frontend run lint
aprovado, sem warnings ESLint

npm --prefix frontend run build
aprovado; avisos preexistentes de `/env.js`, imagem não resolvida, Browserslist e chunks grandes

$env:E2E_SMTP_PORT='28025'; docker compose -f docker-compose-e2e.yml up -d --build --wait
npm --prefix frontend run test:e2e -- e2e/responsive-layout.spec.ts --grep T015 --workers=2
5/5 cenários aprovados em 7,1 s

rg -n 'columnWidths|HeaderTable.*columns' frontend/src/components/global/table frontend/src/pages frontend/src/components/screens
somente props de exportação PDF em `LoanHistory`/`RegisteredUsers` e componentes PDF
```

A regressão E2E percorreu 320×800, 375×812, 767×900, 768×1024 e 1440×900 na jornada de solicitações de cadastro, verificando que as primitivas consolidadas continuam rotuladas e sem overflow. `frontend/tsconfig.app.tsbuildinfo` foi restaurado após o build.

## Limitações

Não há dispositivo físico ou leitor de tela neste ambiente; contratos de acessibilidade, foco e teclado foram exercitados em RTL/Playwright. A suíte completa imprime stacks esperados nas asserções de APIs inválidas, mas encerra com todos os 204 testes aprovados.
