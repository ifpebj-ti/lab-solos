# Evidência T013 — Históricos de mentoria

- Issue: #231 — aberta, atribuída a `nathannmvr`, Project 41 em `In Progress`.
- Escopo: `mentor/MentoringHistory`, `mentee/HistoryMentoring`, testes React, fixtures e cenários T013 da suíte responsiva.
- `tasks.md` fica sob atualização do orquestrador.

## RED

Os testes de contrato caracterizaram os quatro campos do histórico (`Id`, `Data`, `Itens Utilizados` e `Status`), os destinos de detalhe e os dados de perfil por jornada. O baseline legado usava `HeaderTable.columns`, linhas sem `dt`/`dd` e, no perfil mentorado, um contêiner com largura mínima de 800 px.

## GREEN

As duas jornadas agora usam `ResponsiveTable` e `ClickableItemTable`, com colunas rotuladas e células que quebram conteúdo longo. Filtros, ordenação, paginação, estados vazio/carregando, dados de perfil e navegação de detalhe foram preservados. O histórico do mentor continua recebendo `state.id` a partir da turma; o histórico do mentorado usa o `rankID` autenticado.

```text
npm --prefix frontend run test -- --run src/pages/mentor/MentoringHistory.responsive.test.tsx src/pages/mentee/HistoryMentoring.responsive.test.tsx --maxWorkers=1
2 arquivos, 2 testes aprovados
```

## REFACTOR e qualidade

```text
npm --prefix frontend run test -- --run
54 arquivos, 202 testes aprovados

npm --prefix frontend run lint
aprovado, sem warnings ESLint

npm --prefix frontend run build
aprovado durante a reconstrução da imagem E2E; somente avisos preexistentes de `/env.js`, imagem não resolvida, Browserslist e chunks grandes

$env:E2E_SMTP_PORT='28025'; docker compose -f docker-compose-e2e.yml up -d --build --wait
npm --prefix frontend run test:e2e -- e2e/responsive-layout.spec.ts --grep T013 --workers=2
10/10 cenários aprovados em 6,2 s
```

A matriz E2E cobre os perfis mentor e mentorado nos viewports 320×800, 375×812, 767×900, 768×1024 e 1440×900. Foram verificadas a navegação com `state.id`, a consulta autenticada pelo `rankID`, dados longos, labels mobile, alinhamento desktop e ausência de overflow; capturas 320/1440 foram geradas para as duas jornadas. `frontend/tsconfig.app.tsbuildinfo` permanece restaurado após os builds locais.

## Limitações

Não há dispositivo físico ou leitor de tela neste ambiente; roles, labels, foco e teclado foram verificados em RTL/Playwright. A suíte completa imprime stacks esperados nos testes de contratos inválidos, mas encerra com todos os 202 testes aprovados.
