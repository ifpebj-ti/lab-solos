# Evidência T016 — Integração e proteção pré-merge

- Issue: #231 — aberta, atribuída a `nathannmvr`, Project 41 em `In Progress`.
- Escopo: validação cruzada do plano frontend-responsivo; nenhuma implementação adicional nesta tarefa.
- Todas as evidências T001–T015 estão presentes em `evidencias/`.

## Inventário e descoberta

O inventário `rg -n 'global/table/' frontend/src` foi revisado contra os consumidores migrados e os testes de contrato. A auditoria `rg -n 'columnWidths|HeaderTable.*columns'` deixou somente parâmetros de exportação dos documentos PDF, fora das primitivas responsivas. `npm --prefix frontend run test:e2e:list` descobriu 166 testes em quatro arquivos, incluindo smoke, credenciais, contratos de usuário e toda a matriz responsiva T001–T015.

## Validação cruzada

```text
npm --prefix frontend run test -- --run
56 arquivos, 204 testes aprovados

npm --prefix frontend run lint
aprovado, sem warnings ESLint

npm --prefix frontend run build
aprovado; avisos preexistentes de `/env.js`, imagem não resolvida, Browserslist e chunks grandes

$env:E2E_SMTP_PORT='28025'; docker compose -f docker-compose-e2e.yml up -d --build --wait
$env:E2E_SMTP_API_URL='http://127.0.0.1:28025'; npm --prefix frontend run test:e2e
166/166 testes aprovados em 1,5 min

git diff --check
sem erros; apenas avisos de conversão LF/CRLF do working tree
```

A execução completa usou banco e SMTP sintéticos recriados antes da rodada final, com dados de teste somente. A primeira tentativa local falhou apenas porque o teste de credenciais apontou para o SMTP padrão `18025`, reservado pelo Windows; a rodada final usou explicitamente `28025` e passou. Não houve execução de CI remoto.

## Workflow e regras remotas

O workflow `.github/workflows/container-ci.yml` descobre a suíte com `npm run test:e2e`, dentro de `auth-e2e` com limite de 30 minutos; o job `frontend-quality` executa Vitest, lint e build com limite de 20 minutos. A consulta de leitura ao ruleset ativo `develop` (`repos/ifpebj-ti/lab-solos/rulesets/9464959`) confirmou required checks `Frontend quality`, `Backend quality`, `Workflow contracts` e os quatro scans de frontend/backend para `linux/amd64` e `linux/arm64`, com política estrita. Não foram alteradas configurações remotas.

## Complementar e limites

Playwright Chromium em ambiente Windows, cinco larguras (320, 375, 767, 768 e 1440) e estados de carregamento, vazio, erro, dados longos, filtros, paginação, foco e teclado foram cobertos. Não houve dispositivo físico, toque real ou leitor de tela; essa limitação permanece operacional. Capturas e traces usam somente fixtures sintéticas. Artefatos backend preexistentes em `backend/Tests/bin` e `backend/Tests/obj` foram preservados.
