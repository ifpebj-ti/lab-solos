# Evidência T001 — Inventário de fontes e itens sobrepostos

- Data: 2026-09-21
- Issue relacionada: #352, #353, #355 e #356
- Resultado: concluída como inventário local e consulta remota sanitizada; confirmações humanas e publicação permanecem pendentes.

## Verificação inicial (RED/verificação equivalente)

A verificação inicial constatou que `inventario.md` e `evidencias/T001.md` não existiam e que `tasks.md` não possuía registro de execução da T001. Como a tarefa é de inventário/documentação, o RED clássico de comportamento executável não se aplica; a lacuna objetiva foi a ausência do registro rastreável e da matriz de correspondência dos itens citados nas issues.

## Comandos e resultados

Comandos executados a partir da raiz da aplicação:

```text
git status --short
git rev-parse HEAD
git -C ../lab-solos.wiki rev-parse HEAD
gh api user --jq .login
gh issue view 30 --repo ifpebj-ti/lab-solos --json number,title,url,body
gh project item-list 41 --owner ifpebj-ti --format json --limit 100
docker info --format '{{.ServerVersion}} {{.OSType}}'
docker compose version
dotnet --version
python --version
node --version
```

Resultados sanitizados:

- aplicação: `790ee6bd8a1a32ff7e5519d591b73931f537657b`;
- Wiki local: `e0f8ba291ea0d0ee74e33a1c9ce23a6c53d658f4`;
- usuário autenticado: `nathannmvr`;
- #30: encerrada, título `Documentar arquitetura`, sem descrição retornada;
- Project 41: quatro issues da entrega em `In Progress`, atribuídas a `nathannmvr`; rascunhos de arquitetura, classes, modelagem de dados e infraestrutura também foram localizados;
- ferramentas: Docker `29.8.0`/Linux, Compose `v5.5.1`, .NET `8.0.419`, Python `3.14.7`, Node `v24.21.0`;
- imagens locais: `hello-world:latest`; nenhuma imagem versionada do LabOn disponível localmente.

Não foi impresso conteúdo de `.env`, valores de variáveis, logs de containers ou dados de credenciais. O corpo completo das issues não foi copiado para esta evidência; somente critérios e destinos necessários foram resumidos no inventário.

## GREEN

`inventario.md` agora registra referências dos dois repositórios, fontes canônicas/geradas, quatro issues, #30, rascunhos do Project 41, ambiente de teste disponível, versão de imagem não disponível e limites de confidencialidade. O registro distingue base local da Wiki de candidato final e encaminha cada resultado às tarefas proprietárias.

## REFACTOR/verificação final

Foi removida a duplicação entre o inventário e a evidência: o inventário concentra a matriz e o roteiro; esta evidência mantém somente método, resultados sanitizados e pendências. `tasks.md` foi alterado apenas pelo coordenador para marcar T001 como `em andamento`; não foi editado pelo executor desta tarefa. A validação final de espaços em branco e dos dois checkouts deve ser repetida pelo coordenador antes de marcar a tarefa como concluída.

## Pendências preservadas

- Não há confirmação reservada de Nathan sobre credenciais ou Dependabot nesta tarefa.
- Não há instalação do LabOn nem backup/restauração comprovados.
- O SHA final da Wiki, a versão de imagem e a publicação permanecem para T012–T016.

