# Evidência T008 — DER e dicionário físico

- **Tarefa:** T008 — Completar DER e dicionário físico
- **Issue:** [#352 — Documentar arquitetura e modelagem de dados do LabOn na Wiki](https://github.com/ifpebj-ti/lab-solos/issues/352)
- **Data da execução:** 22/09/2026
- **Referência da aplicação:** `790ee6bd8a1a32ff7e5519d591b73931f537657b`
- **Referência local da Wiki usada como contexto:** `e0f8ba291ea0d0ee74e33a1c9ce23a6c53d658f4`
- **Status final no Project 41:** `In Progress`, atribuído a `nathannmvr`; a issue permanece aberta enquanto a integração e os critérios restantes da entrega não forem concluídos.

## Arquivos sob ownership

- [`Arquitetura-e-Modelagem-de-Dados.md`](../../../../../../lab-solos.wiki/Arquitetura-e-Modelagem-de-Dados.md) — DER, cardinalidades, regras de exclusão, distinção entre classes/tabelas e dicionário das sete tabelas.
- `evidencias/T008.md` — este registro sanitizado.

`tasks.md`, `prd.md`, `techspec.md` e `inventario.md` não foram editados.

## Sincronização antes do RED

Foi consultado o estado remoto antes da verificação RED, sem mutação porque a situação já estava persistida:

- `gh api user --jq .login` → `nathannmvr` (exit 0).
- `gh variable get PROJECT_OWNER --repo ifpebj-ti/lab-solos` → `ifpebj-ti` (exit 0).
- `gh variable get PROJECT_NUMBER --repo ifpebj-ti/lab-solos` → `41` (exit 0).
- `gh issue view 352 --repo ifpebj-ti/lab-solos --json number,title,state,author,assignees,projectItems,url` → issue pertencente ao repositório, `OPEN`, assignee `nathannmvr`, item no Project 41 com status `In Progress` (exit 0).
- Consulta filtrada de `gh project item-list 41 --owner ifpebj-ti --format json --limit 100` → item da #352 presente, assignee `nathannmvr`, status `In Progress` (exit 0).

Não houve atribuição adicional, alteração de status, comentário, fechamento ou outra mutação externa.

## RED

Como a tarefa é exclusivamente documental, foi usada uma verificação objetiva em vez de teste unitário: um marcador obrigatório falhando caracteriza a lacuna sem alterar produção. O comando conferiu a presença de `## Modelo físico de dados`, as sete seções de tabelas e `## Dicionário de dados` na página da Wiki.

Resultado inicial: **exit 1**, falha esperada. Foram reportados 9 marcadores ausentes: a seção de modelo físico, as sete tabelas e o dicionário.

## Conferência das fontes

Foram lidos `AppDbContext.cs`, os modelos de usuário, produto, lote, empréstimo, associação, notificação e auditoria, os enums correspondentes e `AppDbContextModelSnapshot.cs`. A conferência confirmou:

- sete tabelas de domínio: `Usuarios`, `Produtos`, `Lotes`, `Emprestimos`, `ProdutosEmprestados`, `Notificacoes` e `LogsAuditoria`;
- herança TPH de `Usuario`/`Administrador`/`Academico` em `Usuarios`, discriminada por `TipoUsuario`;
- herança TPH de `Produto`/`Quimico`/`Vidraria` em `Produtos`, discriminada por `Tipo`;
- `ProdutoEmprestado` como tabela associativa com chave composta e `Quantidade`;
- `Dependente`/`Dependentes` sem entidade correspondente no snapshot;
- `__EFMigrationsHistory` como estrutura técnica de migrações, fora do DER de domínio;
- FKs, nulabilidades e `DeleteBehavior` `Restrict`, `SetNull` e `Cascade` conforme o snapshot.

Não foram coletados registros, valores de credenciais, hashes, tokens ou dados pessoais.

## GREEN

Foi preenchida a página de arquitetura com:

- DER Mermaid compacto com as sete tabelas, chaves e discriminadores, acompanhado de explicação textual;
- cardinalidades e regras de exclusão para solicitante, aprovador, responsável, lote, associação, notificações e auditoria;
- dicionário campo a campo, com tipo PostgreSQL, nulabilidade, PK/FK, enum/discriminador e significado;
- fontes fixadas no SHA completo da aplicação e separação entre classes C#, tabelas TPH e histórico técnico EF;
- descrição sanitizada de `SenhaHash`, `TokenRedefinicaoHash`, `ExigeTrocaSenha` e `VersaoSessao`, sem valores.

Verificação estrutural pós-implementação, com matching literal dos marcadores:

```text
GREEN passed: 7 physical tables, full dictionary markers, relations, credential-field sanitization markers, and 14 pinned source paths verified.
exit=0
```

## REFACTOR

O texto foi padronizado para explicitar a versão do snapshot e criar links individuais para os enums usados no dicionário, sem alterar a semântica do modelo.

Verificação final:

```text
REFACTOR structural validation passed: 7 tables, all required semantic markers, no trailing whitespace, 31 source paths resolve at pinned SHA.
exit=0
```

Verificações adicionais:

- `git diff --check` na aplicação → exit 0; apenas avisos de conversão LF/CRLF da configuração local.
- Check de whitespace específico da página da Wiki → `PASS (0 trailing-whitespace lines)`.
- Checagem de 73 links `blob/<SHA>/<caminho>` da página → `PASS`; todos resolvem no SHA `790ee6bd8a1a32ff7e5519d591b73931f537657b`.
- Spot-check de sanitização → `PASS`; não há atribuições de credenciais ou valores na página.
- `git diff --check` global da Wiki → exit 1 por trailing whitespace preexistente em `Modelagem-de-Ameaça.md`, pertencente à T009 e fora do ownership; o arquivo não foi alterado.
- Prévia visual do Mermaid não foi executada porque o ambiente não possui navegador, `mmdc` ou módulo Mermaid disponível, limitação já observada na T004. A sintaxe e a explicação textual foram revisadas estruturalmente.

Não foram executados testes de aplicação, pois a alteração é exclusivamente documental. Não houve commit, push, merge, PR ou fechamento de issue.
