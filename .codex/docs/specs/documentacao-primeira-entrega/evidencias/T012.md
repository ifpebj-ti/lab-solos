# Evidência T012 — Instalação, primeiro acesso e diagnóstico

- Tarefa: T012 — Executar instalação, primeiro acesso e diagnóstico
- Issue: #353 — Atualizar guia de execução, configuração e operação com Docker
- Data da execução: 2026-09-22
- Status remoto antes e depois: issue aberta, atribuída a nathannmvr, Project 41 em In Progress
- Referência da aplicação: 790ee6bd8a1a32ff7e5519d591b73931f537657b
- Referência HEAD local da Wiki durante o teste: 785b5cb4c8c29e3fec8bdc6a71fac127af21aec4
- Ambiente executado: desenvolvimento, docker-compose-dev.yml, projeto Compose labon-docs-teste
- Dados: somente valores sintéticos descartáveis; nenhum valor de configuração, token, senha ou log bruto foi registrado

## Sincronização antes do RED

As consultas autorizadas foram concluídas antes do RED:

~~~text
gh api user --jq .login -> nathannmvr
gh variable get PROJECT_OWNER --repo ifpebj-ti/lab-solos -> ifpebj-ti
gh variable get PROJECT_NUMBER --repo ifpebj-ti/lab-solos -> 41
gh issue view 353 --repo ifpebj-ti/lab-solos ... -> OPEN, assignee nathannmvr
gh project item-list 41 --owner ifpebj-ti ... -> issue 353, status In Progress, assignee nathannmvr
~~~

A issue pertence ao repositório atual e assignee/status já estavam persistidos. Não houve mutação remota, comentário, fechamento ou alteração do Project.

## Estado inicial e RED

O primeiro teste objetivo verificou a evidência própria da tarefa antes de qualquer edição:

~~~powershell
if (-not (Test-Path .codex/docs/specs/documentacao-primeira-entrega/evidencias/T012.md)) { exit 1 }
~~~

Resultado sanitizado:

~~~text
RED FAIL: evidencia T012 ausente; roteiro operacional ainda sem evidência persistida
exit=1
~~~

Linha de base, sem iniciar serviços:

- docker info -> exit 0, Docker Server 29.8.0, OSType linux, nenhum contêiner pré-existente.
- docker compose version -> exit 0, Compose v5.5.1.
- check_delivery_docs.py --mode editorial -> exit 0.
- check_delivery_docs.py --mode compose -> exit 0.
- git -C ../lab-solos.wiki diff --check -> exit 0.

## GREEN

### Correção documental sob ownership

O único documento canônico corrigido foi o guia da Wiki. As alterações ficaram restritas aos comandos de backup/restauração:

- o docker compose exec de pg_dump passou a indicar explicitamente o serviço db e a continuação Bash;
- o docker run do PostgreSQL de restauração passou a continuar a linha de POSTGRES_PASSWORD;
- os comandos pg_restore e psql passaram a indicar o contêiner de restauração e a continuação Bash.

Não houve alteração no Compose, no backend, no frontend, no banco ou em scripts funcionais.

### Contratos e configuração

Foram criados somente arquivos .env sintéticos temporários em .tmp/t012/labon-docs-teste/.

~~~text
docker compose --env-file <env-dev-sintético> -p labon-docs-teste -f docker-compose-dev.yml config --quiet -> exit 0
docker compose --env-file <env-prod-sintético> -p labon-docs-teste-prod -f docker-compose-prod.yml config --quiet -> exit 0
python .github/scripts/check_delivery_docs.py ... --mode editorial -> exit 0
python .github/scripts/check_delivery_docs.py ... --mode compose -> exit 0
git -C ../lab-solos.wiki diff --check -> exit 0
~~~

Os dois exemplos foram validados estruturalmente; o Compose de produção não foi iniciado.

### Instalação real de desenvolvimento

Antes do up não havia contêineres com os nomes fixos frontend, backend e database, nem listeners nas portas sintéticas escolhidas. O comando solicitado foi executado:

~~~text
docker compose --env-file .tmp/t012/labon-docs-teste/.env.dev -p labon-docs-teste -f docker-compose-dev.yml up -d --build -> exit 0
~~~

Resultado sanitizado de ps:

~~~text
backend   Up; 5080 -> 8080
database  Up (healthy); 55432 -> 5432
frontend  Up; 5173 -> 80
~~~

As imagens locais de frontend/backend foram construídas e postgres:15 foi obtido pelo Docker. Nenhum contêiner de terceiro foi parado ou removido.

### Diagnóstico e saúde

- docker compose ... ps -> exit 0; backend/frontend Up, database healthy.
- docker compose ... exec -T db pg_isready -U <usuario-sintético> -d <banco-sintético> -> exit 0, accepting connections.
- GET http://localhost:5080/api/System/health -> HTTP 200, status healthy 1.0.
- GET http://localhost:5080/health -> HTTP 200.
- GET http://localhost:5173/ -> HTTP 200; resposta HTML recebida.

A inspeção de logs usou somente contagens e padrões sanitizados, sem imprimir linhas brutas. O backend registrou a consulta inicial ausente de __EFMigrationsHistory em banco novo e, em seguida, aplicou as quatro migrações esperadas; depois iniciou ouvindo na porta 8080. Não houve erro persistente de aplicação. O aviso de chaves de Data Protection não persistidas fora do contêiner foi registrado como limite operacional do ambiente descartável, sem alterar código.

### Primeiro acesso e troca obrigatória de senha

O percurso foi executado pela API local usando valores sintéticos carregados do .env temporário; tokens e credenciais não foram impressos:

~~~text
POST /api/Auth/login com seed sintético -> HTTP 200
requiresPasswordChange              -> true
token recebido                       -> sim, comprimento registrado sem conteúdo
POST /api/Auth/change-password       -> HTTP 204
POST /api/Auth/login com senha antiga -> HTTP 401
POST /api/Auth/login com nova senha  -> HTTP 200
requiresPasswordChange após troca   -> false
~~~

Consulta sanitizada do banco confirmou um usuário após o seed:

~~~text
consulta em public."Usuarios" -> exit 0; contagem=1
~~~

Uma primeira tentativa de contagem via psql -c falhou com exit 1 porque o PowerShell removeu as aspas do identificador SQL ao invocar o executável nativo, produzindo diagnóstico sanitizado de relação inexistente. O mesmo procedimento foi repetido via stdin, preservando o identificador quoted, e passou com exit 0 e contagem 1. Não foi necessário alterar código ou banco além do percurso previsto.

### Encerramento normal

Antes do encerramento, pg_isready, health da API e contagem de usuários passaram. Os comandos do projeto foram:

~~~text
docker compose --env-file .tmp/t012/labon-docs-teste/.env.dev -p labon-docs-teste -f docker-compose-dev.yml stop -> exit 0
docker compose --env-file .tmp/t012/labon-docs-teste/.env.dev -p labon-docs-teste -f docker-compose-dev.yml down -> exit 0
~~~

O down não usou --volumes. A inspeção posterior confirmou que labon-docs-teste_pg_data existia e possuía o label com.docker.compose.project=labon-docs-teste, enquanto os contêineres e redes do projeto foram removidos. Assim, o encerramento normal preservou o volume e os dados, sem afetar recursos externos.

## REFACTOR e verificação final

Após o GREEN, a revisão eliminou a ambiguidade sintática dos comandos Bash de backup/restauração. Os contratos foram repetidos:

- validador editorial -> exit 0;
- validador Compose -> exit 0;
- docker-compose-dev.yml config --quiet com env sintético -> exit 0;
- docker-compose-prod.yml config --quiet com env sintético -> exit 0;
- git -C ../lab-solos.wiki diff --check -> exit 0.

## Falhas, limites e pendências

- A falha inicial do psql -c foi de quoting no PowerShell, reproduzida de forma sanitizada e resolvida apenas ajustando a forma do comando de diagnóstico; não indicou falha funcional da aplicação.
- SMTP e DNS/TLS não foram exercitados: o ambiente usa host .invalid sintético. A configuração aceita pelo Compose não foi apresentada como prova de entrega de e-mail ou produção.
- Produção foi validada apenas com config; não foi iniciada, conforme o limite da tarefa.
- Backup/restauração efetivos permanecem para T013; nenhum down --volumes, docker system prune ou limpeza global foi executado.
- O aviso de persistência de chaves de Data Protection é esperado no contêiner descartável e permanece como limite documentado.
- Após confirmar a preservação no encerramento normal, foram removidos somente o volume labon-docs-teste_pg_data, as imagens labon-docs-teste-frontend/backend:latest e os arquivos temporários .tmp/t012, todos exclusivos do teste.
- tasks.md, PRD, techspec.md, inventario.md, manifesto e qualquer arquivo fora do ownership não foram editados.
- Não houve commit, push, merge, Pull Request, comentário ou fechamento de issue.

