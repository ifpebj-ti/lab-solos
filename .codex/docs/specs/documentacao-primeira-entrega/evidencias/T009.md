# Evidência da T009 — ameaças e controles de segurança

- **Tarefa:** T009 — Atualizar ameaças e controles de segurança
- **Issue:** [#355](https://github.com/ifpebj-ti/lab-solos/issues/355)
- **Data da revisão:** 2026-09-22
- **Referência da aplicação:** `790ee6bd8a1a32ff7e5519d591b73931f537657b`
- **Referência local da Wiki usada na conferência:** `e0f8ba291ea0d0ee74e33a1c9ce23a6c53d658f4`
- **Arquivos sob ownership alterados:** `../lab-solos.wiki/Modelagem-de-Ameaça.md`, `../lab-solos.wiki/Guia-de-Boas-Práticas-de-Desenvolvimento-Seguro.md` e este arquivo.

## Pré-condições e sincronização da issue

As dependências T004, T005 e T006 estavam concluídas em `tasks.md`. A própria `tasks.md` já registrava T009 como `em andamento`; por instrução desta execução, ela não foi editada.

Antes do RED, foram executadas as verificações remotas exigidas:

```text
git remote get-url origin
gh repo view --json nameWithOwner --jq .nameWithOwner
gh api user --jq .login
gh variable get PROJECT_OWNER --repo ifpebj-ti/lab-solos
gh variable get PROJECT_NUMBER --repo ifpebj-ti/lab-solos
gh api repos/ifpebj-ti/lab-solos/issues/355 --jq '{number,title,state,assignees:[.assignees[].login],html_url}'
gh project item-list 41 --owner ifpebj-ti --format json --limit 100
```

Resultado sanitizado: repositório `ifpebj-ti/lab-solos`, usuário `nathannmvr`, Project `ifpebj-ti/41`, issue `#355` aberta, assignee `nathannmvr`, item presente no Project com status `In Progress`. O estado já estava persistido; não houve alteração de assignee/status, comentário, fechamento ou outra mutação externa.

O estado local inicial foi conferido com `git status --short` e `git diff --name-status`. As alterações preexistentes em outras páginas da Wiki, nos READMEs operacionais de T005 e em `backend/Tests/bin`/`backend/Tests/obj` foram preservadas. Os três caminhos de ownership da T009 estavam limpos antes da edição.

## RED — verificação documental inicial

Como a T009 altera somente Markdown e não comportamento executável, foi usada uma verificação objetiva de contrato antes da implementação. O comando leu os dois documentos com `System.IO.File.ReadAllText` e conferiu metadados, ativos/atores, fluxos/fronteiras, fontes, matriz, estados, CI e Dependabot.

Resultado inicial, antes das edições:

```text
threat_metadata=False
threat_assets_actors=True
threat_flows_boundaries=False
threat_sources=False
safe_control_matrix=False
safe_statuses=False
safe_ci_current=False
dependabot_pending=False
exit=1
```

Esse RED representa lacunas reais: a modelagem anterior não identificava fronteiras ou fontes rastreáveis, e o guia não separava controles por estado nem refletia os workflows atuais ou a pendência do Dependabot.

## GREEN — implementação mínima

Foram atualizados somente os documentos sob ownership:

- `Modelagem-de-Ameaça.md`: metadados e SHA; ativos A-01–A-07; atores; fluxos F-01–F-08; fronteiras B-01–B-06; ameaças T-01–T-11; tratamentos atuais, limites e fontes canônicas. A página distingue produção/desenvolvimento, API/banco, SMTP, auditoria, releases e CI.
- `Guia-de-Boas-Práticas-de-Desenvolvimento-Seguro.md`: processo vigente de planejamento, desenvolvimento, revisão, CI e operação; descrição atual de `container-ci.yml`, CodeQL e gate de dependências; matriz com controle, estado, justificativa/limite, responsável e evidência.
- Dependabot foi registrado como **planejado e pendente de confirmação de Nathan até T014**. O texto afirma explicitamente que o **CA-009 não está aprovado** e não declara ausência de vulnerabilidades.

Verificações GREEN:

```text
GREEN_CONTRACT exit=0
threat_metadata=True
threat_assets_actors=True
threat_flows_boundaries=True
threat_sources=True
safe_control_matrix=True
safe_statuses=True
safe_ci_current=True
dependabot_pending=True

git -C ../lab-solos.wiki diff --check -- \
  Modelagem-de-Ameaça.md \
  Guia-de-Boas-Práticas-de-Desenvolvimento-Seguro.md
exit=0

git cat-file -e <sha>:<fonte>
verified=20
```

As vinte fontes versionadas referenciadas nos documentos foram conferidas no SHA completo da aplicação. Não foram executados testes de aplicação, lint ou build, pois a tarefa altera apenas documentação; a validação proporcional é editorial e de rastreabilidade.

## REFACTOR — revisão estrutural

Após o GREEN, a estrutura foi revisada para remover recomendações genéricas e centralizar a classificação dos controles. A checagem final confirmou:

```text
one_h1_each=True
all_required_sections=True
matrix_columns=True
states_present=True
dependabot_not_approved=True
sha_links_complete=True
no_known_sensitive_examples=True
exit=0
```

O primeiro agregador dessa checagem retornou `exit=1` por tratar a contagem zero de seções ausentes como falso em PowerShell, embora todos os indicadores impressos fossem verdadeiros. A expressão foi corrigida no próprio comando de verificação, sem alteração adicional dos documentos, e a execução final passou.

Também foram executados:

```text
git -C ../lab-solos.wiki diff --check
exit=0

git -C ../lab-solos.wiki status --short
```

Os avisos de conversão LF/CRLF do Git são informativos. Não há bloqueio de whitespace nos arquivos da T009.

## Critérios e pendências

- **CA-008:** documentado com ativos, atores, fluxos, fronteiras, controles classificados, justificativas, responsáveis e evidências rastreáveis.
- **CA-009:** permanece pendente; a configuração do Dependabot não foi tratada como confirmação de Nathan nem como prova de ausência de vulnerabilidades.
- A execução de CI, a obrigatoriedade dos checks na proteção de `develop`, a instalação operacional e as confirmações reservadas permanecem dependências de tarefas posteriores.

`tasks.md`, PRD, `techspec.md` e `inventario.md` não foram editados. Não houve commit, push, merge, PR, comentário ou fechamento de issue. Nenhuma outra tarefa foi iniciada.
