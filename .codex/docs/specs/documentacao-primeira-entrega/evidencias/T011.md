# Evidência T011 — verificação documental pré-merge

- Data: 2026-09-22
- Tarefa: T011
- Issues relacionadas: #352, #353, #355 e #356
- Repositório: `ifpebj-ti/lab-solos`
- Project: 41, todas as quatro issues abertas, atribuídas a `nathannmvr` e persistidas em `In Progress`
- Referência local da aplicação: `790ee6bd8a1a32ff7e5519d591b73931f537657b`
- Escopo remoto: nenhuma mutação foi necessária; não houve comentário, fechamento de issue ou outra operação externa.

## Arquivos sob ownership

- `.github/workflows/documentation-quality.yml`
- `.github/scripts/tests/test_delivery_docs_workflow.py`
- `.codex/docs/specs/documentacao-primeira-entrega/evidencias/T011.md`

## RED

Antes da implementação, foi criado o teste mínimo de gatilhos e executado:

```text
python -m unittest discover -s .github/scripts/tests -p "test_delivery_docs_workflow.py" -v
```

Resultado: falha esperada, código `1`, com `AssertionError: documentation workflow is missing`.

## GREEN

Foi criado `documentation-quality.yml` com:

- `pull_request` para `develop` em `opened`, `synchronize`, `reopened` e `ready_for_review`;
- `merge_group` e `workflow_dispatch`, sem filtros de caminho;
- permissão global somente `contents: read`;
- ações fixadas por SHA completo;
- leitura e validação de `referenciaWiki` no manifesto;
- checkout do repositório Wiki usando exatamente o SHA produzido pelo manifesto, seguido de comparação de `git rev-parse HEAD`;
- execução dos testes focados, dos modos `editorial` e `compose` e de um caso negativo sem `LABON_IMAGE_VERSION`;
- nenhuma publicação, push, execução de código da Wiki ou operação destrutiva.

Comando e resultado:

```text
python -m unittest discover -s .github/scripts/tests -p "test_delivery_docs*.py" -v
Ran 24 tests in 9.538s
OK (exit 0)
```

Os testes incluem os exemplos sintéticos e reais do validador, o caso negativo de produção e os cinco contratos do workflow.

## REFACTOR

- O contrato foi separado em verificações de gatilhos, permissões/pins, SHA exato da Wiki, comandos de validação e ausência de publicação/execução da Wiki.
- A asserção dos comandos normaliza a quebra YAML de `run: >-` sem enfraquecer os caminhos ou modos exigidos.
- `py_compile` do teste do workflow passou.
- O contrato do workflow manual existente foi preservado e validado:

```text
python -m unittest discover -s .github/scripts/tests -p "test_manual_workflow.py" -v
Ran 5 tests
OK (exit 0)
```

- `git diff --check` nos caminhos da tarefa não produziu diagnóstico; a verificação adicional de trailing whitespace nos dois arquivos de código também passou.

## Regressão e limitações

A regressão Python completa foi executada:

```text
python -m unittest discover -s .github/scripts/tests -p "test_*.py" -v
Ran 339 tests
FAILED (failures=5, errors=3, skipped=2; exit 1)
```

As falhas são preexistentes ou ambientais e não pertencem à T011: `PyYAML` ausente em dois módulos, `.tmp` ausente para um teste de restore, fixture manual divergente do commit informado, quatro testes ESLint bloqueados porque `npx` cancelou a instalação de `eslint@10.11.0`, e dois skips por symlink não permitido no Windows (`WinError 1314`).

O `actionlint` não está disponível no ambiente local. `Get-Command actionlint` não encontrou o executável; o `bash` disponível é um shim WSL sem `/bin/bash`, retornando `execvpe(/bin/bash) failed: No such file or directory`. Por isso, o instalador prescrito e a execução local do actionlint ficaram impossibilitados; a validação será executável no job Linux/CI.

O manifesto definitivo ainda não existe porque T010 permanece em andamento. Assim, a execução local dos comandos `editorial` e `compose` contra o candidato definitivo e a integração real ficam para o contrato do workflow após T010/T015, conforme o plano. Os testes focados exercitaram esses modos com fixtures e exemplos sintéticos, sem substituir a validação do candidato fixado.

Não foram alterados `tasks.md`, PRD, `techspec.md`, `inventario.md` nem arquivos fora do ownership. Não houve commit, push, merge, PR ou fechamento de issue.
