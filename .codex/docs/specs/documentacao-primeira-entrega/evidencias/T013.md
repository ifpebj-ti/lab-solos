# Evidência T013 — Backup, restauração e limites de reversão

- Tarefa: T013 — Comprovar backup, restauração e limites de reversão
- Issue: #353 — Atualizar guia de execução, configuração e operação com Docker
- Data da execução: 2026-09-22
- Status final da tarefa: concluída
- Status remoto informado antes da execução: issue aberta, atribuída a `nathannmvr`, Project 41 em `In Progress`
- Referência da aplicação: `790ee6bd8a1a32ff7e5519d591b73931f537657b`
- Ambiente executado: desenvolvimento, `docker-compose-dev.yml`, projeto Compose `labon-dev`
- Dados e configuração: somente valores sintéticos descartáveis; nenhum segredo real, token, senha ou log bruto foi registrado

## Escopo e sincronização

A T012 estava concluída e esta foi a retomada da mesma T013, não uma nova tarefa. O estado remoto já havia sido conferido pelo coordenador da execução; não foram renovados escopos, feitas alterações no Project, adicionados comentários, fechada a issue ou executadas outras mutações externas.

Foram respeitados os caminhos de ownership da T013. `tasks.md`, PRD, `techspec.md`, `inventario.md`, manifesto, Compose e os READMEs operacionais não foram editados.

## RED e bloqueio anterior

Na primeira retomada, a verificação objetiva falhou porque a evidência ainda não existia:

```text
RED FAIL: T013.md ausente; procedimento de backup/restauração ainda sem evidência persistida
exit=1
```

A tentativa operacional inicial também parou antes do backup porque o PowerShell removeu as aspas do identificador na consulta enviada como argumento:

```text
psql ... --command 'SELECT COUNT(*) FROM "Usuarios";'
exit=1
ERROR sanitizado: relation "usuarios" does not exist
```

Essa causa foi corrigida nesta retomada usando stdin, preservando o SQL quoted.

## GREEN — execução operacional

Foi recriado um `.env` sintético em `.tmp/t013/labon-dev/.env.dev`. O volume anterior do projeto continha somente dados sintéticos da tentativa interrompida; não havia contêineres ativos. A configuração e a subida da stack passaram:

```text
docker compose --env-file .tmp/t013/labon-dev/.env.dev -p labon-dev -f docker-compose-dev.yml config --quiet
exit=0

docker compose --env-file .tmp/t013/labon-dev/.env.dev -p labon-dev -f docker-compose-dev.yml up -d --build
exit=0
serviços: backend Up; database Up (healthy); frontend Up
```

A contagem da origem foi executada via stdin, sem expor valores:

```text
Write-Output 'SELECT COUNT(*) FROM "Usuarios";' | docker compose --env-file .tmp/t013/labon-dev/.env.dev -p labon-dev -f docker-compose-dev.yml exec -T db psql --username <usuário sintético> --dbname <banco sintético> --tuples-only --no-align
exit=0
contagem_origem=1
```

O backup foi criado literalmente em formato customizado e transferido como arquivo binário:

```text
docker compose --env-file .tmp/t013/labon-dev/.env.dev -p labon-dev -f docker-compose-dev.yml exec -T -e PGPASSWORD=<valor sintético> db pg_dump -Fc --file=/tmp/labsolos-t013.dump --username=<usuário sintético> --dbname=<banco sintético>
exit=0

docker compose --env-file .tmp/t013/labon-dev/.env.dev -p labon-dev -f docker-compose-dev.yml cp db:/tmp/labsolos-t013.dump .tmp/t013/backup/labsolos-t013.dump
exit=0

docker compose --env-file .tmp/t013/labon-dev/.env.dev -p labon-dev -f docker-compose-dev.yml exec -T db rm -f /tmp/labsolos-t013.dump
exit=0

docker run --rm --mount type=bind,source=<diretório de backup>,target=/backup,readonly postgres:15 test -s /backup/labsolos-t013.dump
exit=0
```

Foi criado um PostgreSQL 15 separado para a restauração:

```text
docker run --detach --name labon-t013-restore --env POSTGRES_USER=<usuário sintético> --env POSTGRES_PASSWORD=<valor sintético> --env POSTGRES_DB=<banco sintético> postgres:15
exit=0

docker exec labon-t013-restore pg_isready -U <usuário sintético> -d <banco sintético>
exit=0; tentativas=1; espera limitada a 60 segundos

docker cp .tmp/t013/backup/labsolos-t013.dump labon-t013-restore:/tmp/labsolos-t013.dump
exit=0

docker exec labon-t013-restore pg_restore --list /tmp/labsolos-t013.dump
exit=0; entradas=74

docker exec -e PGPASSWORD=<valor sintético> labon-t013-restore pg_restore --exit-on-error --clean --if-exists --no-owner --username <usuário sintético> --dbname <banco sintético> /tmp/labsolos-t013.dump
exit=0

SELECT COUNT(*) FROM "Usuarios"; via stdin para psql no contêiner de restauração
exit=0; contagem_restaurada=1

consultas de acesso/esquema via stdin
exit=0; tabelas_publicas=8; registros___EFMigrationsHistory=4
```

As contagens de origem e restauração coincidiram. O `pg_restore --list` comprovou o conteúdo do dump; o `pg_restore` terminou sem erro; a consulta de tabelas, o histórico de quatro migrações e a contagem de usuários comprovaram esquema, acesso e dados restaurados.

## REFACTOR, limites e encerramento

O procedimento foi revisado sem conversão textual do dump e sem alteração dos documentos canônicos. Os limites foram conferidos contra T005 e os READMEs existentes: rollback de imagens OCI não restaura PostgreSQL nem desfaz migrações; uma baseline não deve ser marcada sem comparar o esquema; não se deve reverter `VersaoSessao`; e falhas de migração exigem backup isolado e decisão de roll-forward/recuperação compatível. Nenhuma reversão destrutiva foi executada.

Após a conferência, somente recursos nomeados do teste foram removidos:

```text
docker rm -f labon-t013-restore
exit=0

docker compose ... -p labon-dev ... stop
exit=0
docker compose ... -p labon-dev ... down
exit=0
```

O `down` foi normal, sem `--volumes`; a inspeção confirmou o label `com.docker.compose.project=labon-dev` antes da remoção exclusiva de `labon-dev_pg_data`, que terminou com exit 0. O dump local e o `.env` temporário foram removidos. As imagens exclusivamente nomeadas `labon-dev-frontend:latest` e `labon-dev-backend:latest` também foram removidas. Não houve `docker system prune`, `docker volume prune`, remoção global, produção ou contêiner ativo ao final.

Uma primeira inspeção do label usando template com aspas internas falhou por quoting do PowerShell; foi repetida com JSON, confirmou o alvo correto e a remoção passou. Isso não afetou a restauração nem os dados já conferidos.

## Validações finais

```text
python .github/scripts/check_delivery_docs.py --repository . --wiki ../lab-solos.wiki --manifest .codex/docs/specs/documentacao-primeira-entrega/documentacao.json --mode editorial
exit=0

python .github/scripts/check_delivery_docs.py --repository . --wiki ../lab-solos.wiki --manifest .codex/docs/specs/documentacao-primeira-entrega/documentacao.json --mode compose
exit=0

git diff --check
git -C ../lab-solos.wiki diff --check
exit=0 em ambos; somente avisos informativos de conversão LF/CRLF
```

Não houve commit, push, merge, Pull Request, comentário, fechamento de issue ou início da T014.
