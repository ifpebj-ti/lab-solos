# Evidência T003 — validador editorial

Data da execução: 2026-09-22
Issues: #352 e #355
Project 41: `In Progress`, atribuídas a `nathannmvr` (estado confirmado antes do RED).
Escopo executado: somente o validador editorial e seus contratos sintéticos.

## RED

- Linha de base: `python -m unittest discover -s .github/scripts/tests -p "test_delivery_docs.py" -v` não encontrou testes (`Ran 0 tests`, código 5), pois a infraestrutura da T003 ainda não existia.
- Primeiro teste: a verificação de página obrigatória ausente falhou contra o esqueleto mínimo (`AssertionError: 0 != 1`). A falha demonstrou o comportamento ausente; não foi uma falha de compilação ou de ambiente.
- A matriz inicial também cobriu links locais quebrados, fragmento ausente, SHA inexistente, metadados, travessia, manifesto inválido, conteúdo sintético suspeito e duplicata.

## GREEN

Implementado em `.github/scripts/check_delivery_docs.py` com biblioteca padrão Python:

- manifesto versão 1 com campos desconhecidos, duplicatas, IDs e caminhos relativos rejeitados;
- referências de aplicação e Wiki conferidas como commits Git existentes;
- páginas atuais e páginas na revisão Git da Wiki conferidas;
- títulos, seções e metadados de data, responsável e referência da aplicação conferidos;
- links locais da Wiki, fragmentos, acentos, percent-encoding e parênteses resolvidos sem busca por substring;
- links `blob/<sha>/<caminho>` do repositório da aplicação conferidos por Git;
- travessia, caminhos absolutos, separadores incompatíveis e links simbólicos rejeitados;
- diagnósticos limitados a localização relativa e regra, sem ecoar conteúdo suspeito;
- códigos 0 (aceito), 1 (violação contratual) e 2 (entrada/ferramenta/infraestrutura ilegível);
- somente leitura: os testes comparam hashes dos arquivos antes e depois da validação.

Resultado GREEN: `python -m unittest discover -s .github/scripts/tests -p "test_delivery_docs.py" -v` — 10 testes passaram.

## REFACTOR

- Separadas leitura do manifesto, resolução segura, validação Git, metadados, headings/âncoras, links e relatório sanitizado.
- Raiz que não é um repositório Git passou a ser classificada como infraestrutura (código 2).
- Adicionado contrato explícito para caminho de página duplicado.
- Fixtures sintéticos foram documentados em `.github/scripts/tests/fixtures/delivery_docs/`; cada teste ainda cria repositórios Git temporários e não depende do manifesto real de T010.

Resultado final focado: 11 testes passaram.

## Comandos de validação

- `python -m py_compile .github/scripts/check_delivery_docs.py .github/scripts/tests/test_delivery_docs.py` — aprovado.
- `python .github/scripts/check_delivery_docs.py --help` — aprovado.
- `python -m unittest discover -s .github/scripts/tests -p "test_delivery_docs.py" -v` — 11/11 aprovados.
- `git diff --check` — aprovado.
- Regressão `python -m unittest discover -s .github/scripts/tests -p "test_*.py" -v`: 326 testes; 5 falhas, 3 erros e 2 skips, todos fora dos caminhos da T003. Preexistências/limitações observadas: PyYAML ausente em `test_automatic_deployment` e `test_trivy_policy`; `.tmp` ausente no teste de restore; divergência de SHA em fixture do teste de publicação manual; e quatro testes de ESLint sem pacote disponível para `npx`.

O modo `compose` permanece reservado à T007; a T003 entrega somente `editorial` e classifica esse modo ainda não implementado como infraestrutura (2). Nenhum script da Wiki foi executado, nenhum manifesto real foi exigido e não houve commit, push, merge, PR, comentário ou fechamento de issue.

`tasks.md` não foi editado.
