# Evidência T004 — Arquitetura e decisões

- **Issue:** [#352](https://github.com/ifpebj-ti/lab-solos/issues/352)
- **Tarefa:** T004
- **Execução:** 21–22/09/2026
- **Referência da aplicação:** `790ee6bd8a1a32ff7e5519d591b73931f537657b`
- **Referência local da Wiki antes da edição:** `e0f8ba291ea0d0ee74e33a1c9ce23a6c53d658f4`
- **Página alterada:** `../lab-solos.wiki/Arquitetura-e-Modelagem-de-Dados.md`
- **Status final no Project 41:** `In Progress`

## Sincronização da issue antes do RED

Consultas executadas antes da verificação RED:

```text
gh api user --jq .login                                      -> nathannmvr
gh variable get PROJECT_OWNER --repo ifpebj-ti/lab-solos      -> ifpebj-ti
gh variable get PROJECT_NUMBER --repo ifpebj-ti/lab-solos     -> 41
gh issue view 352 --repo ifpebj-ti/lab-solos \
  --json number,title,state,url,assignees,projectItems
  -> repositório ifpebj-ti/lab-solos; issue aberta; assignee nathannmvr;
     item no Project LabOn — Desenvolvimento e Qualidade com status In Progress
gh project item-list 41 --owner ifpebj-ti --format json --limit 100
  -> item da #352 presente, assignee nathannmvr, status In Progress
```

A issue já estava no Project configurado, atribuída ao usuário autenticado e com `In Progress` persistido. Não houve mutação externa adicional; não foi criado comentário, não foi fechada a issue e não houve alteração de outros campos.

## Conferência de equivalente

No checkout local da Wiki, `rg --files` e a busca por termos de arquitetura/modelagem não encontraram uma página canônica equivalente. O caminho `Arquitetura-e-Modelagem-de-Dados.md` estava ausente. Os rascunhos citados na issue — “Definição da arquitetura”, “Criar diagrama de Classes” e “Documento de Modelagem de Dados” — aparecem como referências do Project/issue, não como páginas publicadas reutilizáveis. A página nova consolida somente a arquitetura de execução; o DER/dicionário foi explicitamente reservado à T008.

## RED — verificação documental inicial

Verificação objetiva executada antes da edição:

```powershell
$target = Join-Path $wiki 'Arquitetura-e-Modelagem-de-Dados.md'
if (-not (Test-Path -LiteralPath $target)) {
    Write-Error 'RED esperado: página de arquitetura inexistente; CA-001 não pode ser verificado.'
    exit 1
}
```

Resultado: **exit 1**, com a mensagem `RED esperado: página de arquitetura inexistente; CA-001 não pode ser verificado.`. A falha demonstra a lacuna documental, não um erro acidental de compilação ou ambiente.

Linha de base adicional:

```text
git -C ../lab-solos.wiki diff --check -> exit 0
```

O Git emitiu apenas avisos preexistentes de conversão LF/CRLF nas páginas já modificadas `Guia-de-Execução-e-Configuração-com-Docker-e-Docker-Compose.md` e `Guia-de-Testes-de-Usuário.md`; elas não foram editadas pela T004.

## GREEN — conteúdo mínimo implementado

Foi criada a página `../lab-solos.wiki/Arquitetura-e-Modelagem-de-Dados.md` com:

- diagrama Mermaid e explicação textual equivalente;
- navegador, frontend/Nginx, API ASP.NET Core, PostgreSQL, Caddy, SMTP e atualizador OCI;
- separação explícita dos Compose de desenvolvimento e produção;
- redes públicas/privadas, fronteiras externas e administração da VM;
- decisões rastreadas para `Caddyfile`, Compose, `Program.cs`, `BaseApi.tsx`, serviço de SMTP e scripts OCI;
- distinção precisa entre `/health` e `/api/System/health`;
- links `blob/<SHA completo>` para as fontes do commit de referência;
- reserva explícita do DER e dicionário físico para T008, sem declarar tabelas ou relações concluídas.

Verificação executada após a criação:

```text
marcadores obrigatórios encontrados: 16
referências blob/790ee6bd8a1a32ff7e5519d591b73931f537657b/: 37
DER-reserved=ok
git -C ../lab-solos.wiki diff --check -> exit 0
```

Também foi conferida a existência no SHA fixado das fontes usadas: 16 caminhos, incluindo os dois Compose, `Caddyfile`, `Program.cs`, `SystemController.cs`, `BaseApi.tsx`, Nginx/entrypoint, `EmailService.cs`, README/scripts/systemd da OCI.

## REFACTOR — revisão editorial e estrutural

Após o GREEN, o diagrama foi simplificado para apontar o atualizador ao nó concreto `caddy`, em vez de depender do identificador do subgrafo de produção. O cabeçalho foi atualizado para a data corrente da revisão e foram adicionados `bootstrap-app.sh` e `install-auto-update.sh` à rastreabilidade dos scripts OCI.

Verificação final do ciclo:

```text
cercas Markdown: 2, balanceadas
fontes verificadas no SHA: 16
DER-reserved=ok
git -C ../lab-solos.wiki diff --check -> exit 0
```

Foi feita revisão estrutural do bloco Mermaid extraído — `flowchart`, dois subgrafos, nós, arestas e rótulos balanceados — e revisão textual equivalente. Não há `mmdc`, módulo Mermaid local ou navegador disponível nesta máquina para gerar uma imagem de prévia; a renderização visual permanece uma limitação a conferir na prévia/publicação da Wiki em T015/T016.

## Validações, preexistências e bloqueios

- `git -C ../lab-solos.wiki diff --check`: aprovado; somente os avisos LF/CRLF das páginas preexistentes foram emitidos.
- Verificação de referências com `git cat-file -e <SHA>:<caminho>`: aprovada para as 16 fontes documentadas.
- Não foram executados build, lint ou testes da aplicação: a mudança é exclusivamente Markdown na Wiki e evidência Markdown no repositório da aplicação; nenhum código funcional foi alterado.
- Não foi executada instalação, teste de login, envio SMTP, validação de DNS/TLS, backup/restauração ou prévia visual real; esses procedimentos pertencem às tarefas operacionais/integradoras posteriores.
- Alterações preexistentes em `backend/Tests/bin`, `backend/Tests/obj`, specs locais e páginas da Wiki foram preservadas.
- Não houve commit, push, merge, PR ou fechamento de issue.
- `tasks.md`, PRD, techspec e inventário não foram editados.
