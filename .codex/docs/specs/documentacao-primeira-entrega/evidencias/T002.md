# Evidência T002 — Saneamento de exemplos e preparação reservada

- Data: 2026-09-21
- Issue relacionada: #356
- Resultado: concluída a sanitização documental; CA-010 permanece pendente até confirmação reservada em T014.

## Verificação inicial (RED)

A inspeção delimitada encontrou, no guia de execução da Wiki, uma seção de acesso de produção com material de credencial e valores de configuração que não podiam permanecer publicados. Também encontrou endereços de e-mail fora de exemplos sintéticos em cenários documentais. Os valores não foram reproduzidos nesta evidência.

Como a tarefa é documental, o RED foi a falha objetiva da revisão de confidencialidade, não um teste de comportamento: a regra esperada era que materiais públicos contivessem somente placeholders ou exemplos sintéticos e o conjunto inicial não satisfazia essa regra.

## GREEN

Foram alteradas somente as fontes da Wiki sob a responsabilidade desta tarefa:

- `Guia-de-Execução-e-Configuração-com-Docker-e-Docker-Compose.md`: removida a seção de credenciais de produção; o guia agora orienta o provisionamento por `SEED_ADMIN_EMAIL`/`SEED_ADMIN_PASSWORD` sem compartilhar valores. O bloco local usa valores sintéticos e domínio `example.invalid`.
- `Guia-de-Testes-de-Usuário.md`: endereços usados nos cenários foram substituídos por um endereço sintético `example.invalid`.

Não foram alterados `.env` privados, código funcional, scripts de infraestrutura fora do escopo nem credenciais reais em evidências. Os procedimentos de provisionamento continuam apontando para o responsável pelo ambiente.

## Verificações

Comandos executados:

```text
git -C ../lab-solos.wiki diff --check
python .github/scripts/check_manual.py --source docs/manual
```

Resultados:

- `git -C ../lab-solos.wiki diff --check`: passou; apenas avisos de normalização futura de LF/CRLF foram emitidos pelo Git.
- `check_manual.py`: `Manual aprovado: manifesto e páginas coerentes.`
- varredura sanitizada da Wiki: `wiki_non_synthetic_email_count=0` e `wiki_non_synthetic_assignment_count=0` para os campos documentais verificados.
- a busca por marcador conhecido do material removido não encontrou ocorrência.

## REFACTOR

Os exemplos foram uniformizados para indicar explicitamente quando são sintéticos e para separar configuração local de credenciais de produção. A evidência registra regra, caminho e resultado sem copiar conteúdo sensível.

## Estado externo e pendências

Antes do fechamento da tarefa, a issue #356 foi conferida como aberta, atribuída a `nathannmvr` e com item do Project 41 em `In Progress`; as variáveis de configuração do Project retornaram `ifpebj-ti` e `41`. Nenhum comentário, fechamento ou outra mutação externa foi realizado.

A revisão/invalidação reservada de credenciais e sessões ainda não foi confirmada por Nathan. Portanto, T002 não encerra CA-010 nem autoriza concluir a issue; essa confirmação é responsabilidade de T014.

